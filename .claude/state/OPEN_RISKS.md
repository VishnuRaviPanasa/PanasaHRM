# Open Risks and Deferred Items

Reviewed at every slice close. Anything still open after two slices gets escalated to the human.

| ID | Item | Why it matters | Owner | Review by |
|---|---|---|---|---|
| OR-01 | **Handbook contradictions - no longer blockers.** C3 and C8 answered by HR; C1, C2, C4, C5, C6, C12 are now **settings with badged defaults** (DEC-019/020). Only **C7** remains genuinely absent - there is no overtime policy to configure from | 16 fields carry engineering defaults badged `unconfirmed`. Nothing is blocked, but a badged value must not be presented as settled policy | Human (HR) | Confirm via the settings screen |
| OR-08 | **Two residual sub-questions from the C3/C8 answers**: (a) is the 6-month cap window calendar halves or rolling? (b) should `full_day_minutes` be 465 rather than 480, so the 15-min grace does not cost half a day? Defaults chosen for both; each is a one-line change | Wrong defaults would be quietly wrong rather than loudly wrong | Human (HR) | Before Phase 8 |
| ~~OR-02~~ | ~~2026/2027 holiday calendars missing~~ **RESOLVED 2026-09-08** - 2026 recorded from live GreytHR in `docs/requirements/holiday-calendar-2026.md`. 2027 still outstanding (H-06) | - | - | Closed |
| OR-03 | **No named legal/compliance contact** for DPDP sign-off | DPDP compliance is currently an accepted risk with no owner | Human | Before go-live |
| OR-04 | **Nothing pushed to the remote** - the sandbox has no network access | The repo exists only locally | Human | Next non-sandboxed run |
| OR-05 | **ESLint not configured** - deferred until there is code to lint (DEC-002) | Module-boundary enforcement (Must-Know Rule 8) is currently documentation, not mechanism | Claude | Phase 2 |
| OR-06 | **Holiday election questions H-01..H-06** raised by the 2026 calendar - notably the optional allowance (2 in 2025, at least 3 in 2026) and what happens to leave already approved on a date that later becomes a holiday | The election cap is a hard input to the leave engine; the retroactive-holiday case is a ledger reversal path | Human (HR) | Before Phase 6 |
| OR-09 | **ADR-0012 (payroll) is BLOCKED on Q10** - build payroll here, or buy/integrate? If "buy", the ADR should be **retired or voided, not amended** (DEC-026). Its `Reconsider when: Never` has been removed | An Accepted ADR describing a module that is never built would sit at the top of the authority order misdirecting every later decision | Human | Before Phase 10; sooner if GreytHR retirement is decided (OR-07) |
| OR-10 | **§9 residue - now delivered, one item excepted.** ADR-0006 (policy version on the ledger row), ADR-0009 (UNIQUE (tenant_id, subject); CHECK bound to `password_hash` not a flag; break-glass exception named), ADR-0016 (money type, divisor as effective-dated policy, `comp_viewer` restated as `work.effort_cost:read` + glossary entry) and ADR-0008 (ordering, dead-letter, table name) are all present. **Still outstanding: ADR-0008's retention/dead-letter MIGRATION**, which is OR-11(a) - the ADR now specifies it, the schema does not implement it | Kept open deliberately: an amendment that specifies a control is not the same as the control existing | Claude | Before ADR-0008 is accepted |
| OR-11 | **Two of three review-flagged migrations remain.** ~~Rule 3 enforcement~~ **DONE** - `0004_effective_dated_immutability.sql` plus `0005_rule3_perimeter.sql` (business date, back-dated INSERT, TRUNCATE, ENABLE ALWAYS, pinned search_path); 24 checks across R1-R13 and P1-P11. Still open: **(a)** outbox retention + dead-letter + immutability extended to `actor_user_id`/`correlation_id`; **(b)** `company.timezone` and `leave_type` derivation flags moved to effective-dated, and the policy resolvers made to RAISE outside the policy epoch | (a) the outbox cannot be pruned at all - DELETE is blocked and the table is not partitioned, so it grows without bound with no remediation path. (b) two historical derivation inputs sit in mutable storage, and a pre-2020 date silently resolves to NULL | Claude | Before ADR-0008 and ADR-0019 are accepted |
| OR-14 | **ADR-0013's principal accepted risk has no control behind it**: no backup script, no restore runbook, no verified restore drill, and the PGDATA path is the non-obvious `/var/lib/postgresql/18/docker` (DEC-010) | The single-VM deployment is accepted *on the basis* that off-host encrypted backups bound the loss. Until they exist the trade-off is unexamined rather than considered. Phase 9 gates go-live on a completed restore drill | Human + Claude | Before Phase 9; sooner if real data lands |
| OR-12 | **`docs/privacy/data-inventory.md` does not exist, and migrations 0001-0003 already shipped unclassified personal-data columns** (`source_ip`, subject/actor ids); `audit_column_policy` holds 0 rows | CLAUDE.md's Forbidden Actions bar adding a personal-data column without a classification - so the rule is **live and currently unsatisfiable**, and the work module (Phase 7) cannot be built without violating it | Human + Claude | Before Phase 4 adds personal-data columns |
| OR-13 | **`docs/standards/api-conventions.md` still missing.** ADR-0004 now records exactly what is undecided: idempotency-key replay semantics (window, storage, repeated key with a different body) and ETag derivation | T9 (NestJS bootstrap) implements RFC 9457, request IDs and OpenAPI. Writing the convention after the endpoints means documenting whatever got built | Claude | Before T9 |
| OR-07 | **GreytHR covers more ground than assumed** - its ESS today includes Leave, Attendance, Salary, Document Center, People, Helpdesk, Request Hub and Workflow Delegates. Replacing it is a materially larger scope than complementing it | Directly affects O12 (retire or coexist) and the MVP cut line | Human | Before Phase 7 |
| OR-15 | **Nothing calls `fn_refresh_due_employment_status()`** - there is no scheduler, so a future-dated joining or exit never materialises into the cached `employee` columns when its date arrives | A pre-boarding hire stays `pre_boarding` after their joining date and a scheduled leaver stays `on_notice` after their last working day, until something calls the catch-up. `fn_employment_status_asof(employee, fn_business_date())` is always correct and needs no job, so the mitigation is to read through the resolver rather than the cache - but every existing query reads the cache | Claude | Before Module 1's endpoints ship |
| OR-16 | **`blood_group` has no confirmed purpose and `emergency_contact_*` is third-party personal data with no notice mechanism** (DEC-035) | Health data collected without a stated purpose is precisely what purpose limitation forbids, and the emergency contact never consented to being in our database. Neither is a developer's call to settle | Human (HR) + legal (OR-03) | Before Phase 9 |
| ~~OR-17~~ | **RESOLVED 2026-09-08 (project_member half).** `project_member` is effective-dated as of migration 0019 (DEC-057), so the project scope graph decays. **Still open**: `rbac-rules.md` names tables that do not exist - `reporting_relationship`, `reporting_closure_current` - and asserts `project_member` is effective-dated when the committed table has only `created_at` and a UNIQUE constraint | A context file is authority for anyone building authorization, so a reader implements against a schema that was never built, or duplicates an edge that already exists (DEC-032). `project_member` not being effective-dated additionally means the WORK scope graph cannot decay at all: a removed project member either keeps access forever or loses their own history | Claude | Before the authz spine is written |
| ~~OR-18~~ | **RESOLVED 2026-09-09 (DEC-089).** Personal data now flows through `people.employee.personal.read` and the default-deny field registry: HR reads address, date of birth and phone; `emergency_contact_*` and `blood_group` stay SELF_ONLY; a line manager still gets nothing; and the edit endpoint derives its writable set from the same mask, so a field you cannot read is a field you cannot set. `privacy:test` re-pinned to the new posture with three checks where it had one. **Original text:** `hr_admin` cannot see any employee's personal data (DEC-036), and there is no self-service edit path either | HR genuinely needs address, emergency contact and date of birth to do its job, and the interim allowlist denies it rather than adding another inline role check. So Module 1's profile screen is read-only and HR is currently worse off than a spreadsheet for those fields. This is a deliberate default-deny, not an oversight - but it is a functional regression against what HR needs and must not be left standing | Claude | With packages/authz fieldMask |
| OR-19 | **The route retrofit is PARTIAL. `documents` and `settings` now enforce `AuthorizationService`; the rest do not, so Must-Know Rule 1 is still violated in `SessionGuard`** | `packages/authz` exists and is verified, but the controllers still carry `@Authenticated('manager','hr_admin')` and the guard still compares roles - outside `packages/authz`, which Rule 1 forbids. It now compares `actor.roles` rather than the superseded single column, so revocation is honoured immediately, and the inline SQL role checks (`$2 = 'hr_admin' OR em.manager_id = $1`) are still in place in hr.ts/leave.ts/work.ts. Settings was the proof this matters: it let a MANAGER read company policy while the matrix said deny (DEC-053). Remaining: hr.ts (dashboard, employees), leave.ts, and most of work.ts - including the inline SQL role checks. `work.ts`'s timesheet DECIDE route is now retrofitted (DEC-058), and its `if (me.role !== 'hr_admin')` is gone. The pattern is established in `documents.ts`, `settings.ts` and `authz.ts` | Claude | Next slice |
| OR-20 | **Entra ID / OIDC is not built, and cannot be verified from this environment** | `user_identity` is ready for it with `UNIQUE (tenant_id, subject)` and the MFA-downgrade rails, but there is no authorization-code + PKCE flow, no JWKS cache and no discovery. It is also unverifiable here: the sandbox has no network, there is no tenant, and CLAUDE.md forbids writing `.env*` or `secrets/**`. Writing the flow blind would produce code that looks finished and has never executed | Human + Claude | Needs a tenant, a redirect URI and a non-sandboxed run |
| OR-21 | **Sessions are still PostgreSQL rows and passwords are still scrypt** | ADR-0010 makes Redis authoritative for session existence so deleting a key ends a session immediately; Redis is now running (55379, DEC-037) but nothing uses it, so `fn_revoke_sessions_for_user` is the only kill-switch. ADR-0009 specifies argon2id; `app_user.password_algo` exists precisely so scrypt and argon2id can coexist during a rehash-on-login migration, but argon2id needs a dependency and the semaphore ADR-0009 describes. The cookie is also `hrm_session` without `Secure`, not `__Host-hrm_session` - both change together with TLS | Claude | Before go-live |
| ~~OR-22~~ | **MOSTLY RESOLVED 2026-09-09 (DEC-087).** Departments and designations now have full masters - endpoints, UI at `/organisation`, as-of structure, rename/move/retire, and 49 checks including the forbidden paths. **Still open:** TEAMS have no UI (`org.team.manage` and `org.team.member.manage` exist and are tested in the matrix, but nothing renders them), and there is no org CHART - the master is a table, not a tree diagram. **Original text:** Organisation structure has no endpoints or UI - no department tree screen, no team management, no org chart, and the existing `/employees` directory still shows a flat department name rather than a hierarchy | The schema, resolvers and authz actions are all in place and verified (0017, 18 checks; `org.*` actions in the matrix), so this is presentation work rather than design work. But until it exists, HR cannot record a reorganisation through the product - only through SQL | Claude | With the route retrofit (OR-19) |
| OR-23 | **There is no virus scanner, so the quarantine gate has nothing intelligent behind it** | scan_status is a real state machine and the database refuses to promote anything not clean (0018, check D6) - but reaching clean is currently an administrative act by HR, not a verdict from a scanner. So the control is structurally sound and epistemically empty: HR clicking clean proves only that HR clicked it. Until a scanner drives it, an infected file that HR clears IS servable | Human + Claude | Before real employee documents are loaded |
| OR-24 | **The two-bucket quarantine SPLIT is not built, and MinIO's own posture is unestablished** | security-guidelines.md wants an upload to land in a quarantine bucket and be promoted to a clean one; what exists is the database gate instead, which prevents SERVING but not storage-layer access. Additionally: object content is the only data in the inventory that leaves PostgreSQL, so MinIO credentials are equivalent to a bulk document export - and its encryption-at-rest, retention and access-log posture have not been established. Also missing: image re-encoding through sharp to strip EXIF (a photographed ID card carries GPS) and destroy polyglot payloads | Claude | Before go-live |
| OR-25 | **A RESTRICTED document is not visible to the employee it is about** | An employee cannot retrieve their own appraisal, contract, offer letter or disciplinary record through this API. That is the narrow default chosen deliberately, not a considered position - whether an employee has a right of access to their own appraisal is an HR and legal question. Being wrong in this direction costs a foreclosed feature; being wrong in the other could disclose a disciplinary record prematurely | Human (HR) + legal (OR-03) | Before go-live |
| OR-26 | **Tasks are READ-ONLY: the report now surfaces work assigned to nobody, and there is no endpoint to assign it** | `fn_project_task_status` exists specifically to make unassigned work visible, and it does - two open tasks with no owner. But `task` has no create, edit or assign endpoint anywhere in `apps/api`, so the only way to act on the finding is SQL. A report that raises a question the product cannot answer is a worse experience than not raising it, because the reader assumes an affordance exists and goes looking. The schema and the `fn_task_assignee_is_member` guard are both in place, so this is endpoint work, not design work | Claude | With Module 9's write endpoints |
| OR-27 | **No report can be exported, and none paginates** | Module 14 asks for "HR operational reports", and an HR operational report in practice gets sent to somebody - payroll, an auditor, a board pack. Every report here is screen-only. Export is also the point where scope correctness becomes easiest to lose: the natural implementation streams the same query to CSV, and if it is written as a separate path rather than reusing `gate()`, DEC-060's guarantee is silently gone. Pagination is a genuine non-issue at five employees and worth stating so nobody adds an unscoped `COUNT(*)` total later - `rbac-rules.md` names pagination totals as a leak channel specifically | Claude | Before HR uses reports operationally |
| OR-28 | **Nobody can issue a payslip for the person the payroll administrator reports to** | DEC-072 applies the `is_ancestor_of_actor` deny-override to `payroll.payslip.manage` as well as `.read`, because entering figures you are not allowed to look at is incoherent. The consequence is real: if the only `hr_admin` reports to the CEO, no one in the system can draw the CEO's payslip. `rbac-rules.md` mandates the read rule, so the rule stays; **who pays the person the payroll administrator reports to is a segregation-of-duties question for a human**, not a developer's call. The likely answer is a second holder of `finance` outside that reporting line, which is an organisational decision rather than a code change | Human (HR + Finance) | Before payroll runs for real |
| OR-29 | **Tier 1 payslip isolation is STRUCTURAL BUT NOT YET EFFECTIVE, because the API connects as the database owner** | 0024 revokes `hrm_app` from every payslip table and creates `hrm_payroll` with the grants (DEC-073), and check PS20 proves the revoke holds. But `apps/api/src/db.ts` still connects as the OWNER - a documented Track B task blocked on P3-7 - and an owner bypasses every grant. So today the separation is a correctly-built rail that nothing is standing on: a SQL injection anywhere in the API could still read the salary register. This is not a regression (it is true of every table) but it matters more here than anywhere else, and it is the reason the payroll module has its own role ready rather than sharing `hrm_app`. Switching the connection is now worth more than it was before payslips existed | Claude | With the hrm_app switch |
| OR-30 | **A payslip PDF is cleared for serving without any scanner having looked at it** | `POST /payslips/:id/document` marks the uploaded version `clean` in the same request, and records `payroll.payslip.document.cleared_without_scanner` in the audit trail so it is visible rather than implied. That is the same administrative assertion `documents.ts` makes when HR presses its scan button (OR-23), taken in one step instead of two - the threat profile is different from an employee-uploaded ID scan, since a payslip PDF comes from the payroll system - but it is still an assertion and not a verdict. **When a scanner lands, that line in `payroll.ts` is what stops doing this.** Until then the quarantine gate is structurally sound and epistemically empty for payslips | Human + Claude | Before real payslips are loaded |
| OR-31 | **The 8-year payslip retention was chosen for consistency, not derived from statute** | `document_type.payslip.retention_years = 8` matches the existing 8-year document types rather than any Indian statutory retention period for wage records, and the payslip tables inherit it. `docs/privacy/data-inventory.md` records this openly. Wage records are ordinarily retained under statute regardless of consent, but which statute and for how long is not an engineering judgement - and it interacts with a DPDP Act deletion request in a way nobody here can settle. Same class as OR-03: needs a named reviewer | Human (legal) + HR | Before go-live |
| OR-32 | **The employee lifecycle has no UI: nobody can confirm, resign, terminate or re-hire through the product** | The masters cover create, edit and MOVE. They do not cover the lifecycle transitions, and the gap is now conspicuous because everything under it is built: 0014's FSM holds the legal moves as data, `people.lifecycle.transition` exists with a policy and matrix cells, and `fn_refresh_employment_status` derives the cached columns. What is missing is endpoints and screens for probation confirmation, resignation with a notice period, termination with an exit type, and the exit-day revocation that `fn_revoke_access_on_exit` already implements. Until then an employee can be created but never offboarded except through SQL - and OR-15 (nothing calls the catch-up for future-dated events) bites hardest exactly there | Claude | Next slice |
| OR-33 | **A department can be left UNPLACED, and the master reports it rather than preventing it** | `department` and `department_period` are separate tables, so a department with no placement covering a given date is representable - the list endpoint returns `unplaced: true` for it and the UI badges it. The create endpoint always writes an initial placement, so the product does not produce one; but closing a placement without opening another would, and nothing refuses that. Whether an unplaced department should be impossible or is a legitimate "parked" state is a modelling question rather than a bug: a department mid-reorganisation arguably has no parent for a period. Recorded so the answer is chosen rather than inherited from what the schema happened to allow | Claude | Before the org chart is built |
