# Decision Log (DEC-NNN)

Non-architectural decisions: waived findings, deferred scope, accepted risks, convention picks.
**Architectural** decisions go in `docs/adr/` instead.

Every waiver needs a reason, an owner and a review date. Silence is not a waiver.

| ID | Date | Decision | Reason | Owner | Review by |
|---|---|---|---|---|---|
| DEC-001 | 2026-09-08 | `CLAUDE.md` carries **no phase or status line** | The plan (section 24.2) requires zero mutable state in `CLAUDE.md`; a hardcoded phase is exactly the line that goes stale and misleads a future session. Status lives in `.claude/state/`. This supersedes the plan section 48 wording for Task 1 | Claude | - |
| DEC-002 | 2026-09-08 | **ESLint deferred to Phase 2** | There is no TypeScript to lint, and `eslint-plugin-boundaries` needs the module paths that Phase 2 creates. Configuring it now would be speculative and untested | Claude | Phase 2 |
| DEC-003 | 2026-09-08 | **Hooks deferred to Task 2** | Hooks ship together with their scripts so each can be tested the moment it is added. A hook entry pointing at a missing script fails silently, which is worse than no hook | Claude | Task 2 |
| DEC-004 | 2026-09-08 | **Hook scripts are Node, not Python** | The org precedent (Thredd) used Python with a shell shim, because `python3` on Windows is often a Microsoft Store stub that fails an import test. `node` has no such ambiguity and is already the project runtime, so hooks are one language fewer to maintain | Claude | - |
| DEC-005 | 2026-09-08 | **`confidence_score` is NOT a gate input** (envelope v2) | Self-reported confidence only catches the agent honest enough to admit uncertainty. The gate is `critical_count == 0` plus deterministic checks. Confidence is kept as a triage hint for reading order | Claude | - |
| DEC-006 | 2026-09-08 | **Invocable workflows are skills, not `.claude/commands/`** | Skills are the current mechanism, take precedence where both exist, and support `!` shell injection (used by `session-brief` to ground the model in live state) and `disable-model-invocation` for side-effecting flows | Claude | - |
| DEC-007 | 2026-09-08 | **Only two agents may write, and neither writes application code** | `migration-author` is confined to migrations and the schema mirror; `test-author` to tests and fixtures. With no peer reviewer, an agent writing business logic that another agent approves is a closed loop with no human in it | Claude | Reconsider if a second developer joins |
| DEC-008 | 2026-09-08 | **Stop-hook typecheck and push-gate artifact deferred to Phase 2** | There is no TypeScript to check and no code to gate. A hook that no-ops is indistinguishable from one that is broken, which is worse than a documented absence | Claude | Phase 2 |
| DEC-009 | 2026-09-08 | **Dev-stack host ports default to a high range** (55432, 56379, 59000/1, 58080), overridable by env var | A local PostgreSQL already owns 5432 on at least one developer machine, so the standard port makes `docker compose up` fail out of the box. All bind to 127.0.0.1 only | Claude | - |
| DEC-010 | 2026-09-08 | **Postgres volume mounts at `/var/lib/postgresql`, not `/var/lib/postgresql/data`** | PG18's image changed this. The image places data in a major-version subdirectory so `pg_upgrade --link` works without mount-boundary problems. Mounting at `.../data` makes an 18+ container refuse to start - found by it actually refusing to start | Claude | - |
| DEC-011 | 2026-09-08 | **Migrations are forward-only; there is no `down`** | Production rollback is deploying the previous image against the new schema, which is what makes expand/contract non-optional rather than advisory. A down-migration is a comforting fiction nobody tests, and it cannot restore dropped data anyway | Claude | - |
| DEC-012 | 2026-09-08 | **The migration runner enforces checksums** and refuses to apply anything if an already-applied migration was edited | Editing an applied migration is how two environments silently diverge, with nothing reporting it | Claude | - |
| DEC-013 | 2026-09-08 | **The migration runner uses `psql` only, not the app's node_modules** | It must work before `npm install`, in CI, and during an incident. `psql` is already required to operate this system | Claude | - |
| DEC-014 | 2026-09-08 | **Migration checksums normalise line endings before hashing** | A Windows checkout has CRLF and a Linux/CI checkout has LF, so hashing raw bytes made the SAME migration report drift on every CI run. Found by it reporting drift immediately after a clean apply | Claude | - |
| DEC-015 | 2026-09-08 | **`psql` output is CR-stripped before parsing** | Same CRLF class, second location: per-row values kept a trailing CR, so a stored checksum never compared equal to the recomputed one | Claude | - |
| DEC-016 | 2026-09-08 | **`full_day_min_minutes` defaults to 465, not 480**, and a CHECK enforces `full_day <= standard_day - grace` | With an exactly-8h standard day, a strict 480 threshold classifies anyone using the 15-minute grace as a half day - making the grace cost half a day's pay. The constraint makes that configuration impossible to save from the settings screen. Pending HR confirmation (OR-08) | Claude | On HR confirmation |
| DEC-017 | 2026-09-08 | **`probation_months` is NULL, meaning unknown - not zero** | C4 is unanswered. A default of 0 would silently compute confirmed-employee entitlement for probationers. Callers must refuse rather than assume | Claude | On C4 |
| DEC-018 | 2026-09-08 | **The 6-month leave cap window is calendar halves** (Jan-Jun, Jul-Dec), not rolling | Aligns with the Jan-Dec leave year already established, and is far cheaper to compute and to explain to an employee. Not covered by HR's C3 answer | Claude | On HR confirmation |
| DEC-019 | 2026-09-08 | **C1, C2, C4, C5, C6, C12 become settings rather than blockers** | Each is a value or a well-defined branch, not a new algorithm. The one exception is C4's accrual method, which is three genuinely different computations - configurable anyway, because HR cannot choose between them in the abstract and the screen can show the arithmetic | Claude | - |
| DEC-020 | 2026-09-08 | **`unconfirmed_fields` badges engineering defaults** on every policy row, validated by trigger | Making a value configurable moves the risk from "the developer guessed" to "HR never looked and the default became policy" - which is worse, because it looks settled. 16 fields are currently badged | Claude | As HR confirms each |
| DEC-021 | 2026-09-08 | **Half-configured policy is rejected by CHECK constraints** - a cap size with no window, carry-forward enabled with a zero cap, negative balance disallowed with a non-zero allowance | These configurations look active in a UI and silently do nothing. A settings screen makes that class of mistake much more likely, so the database refuses them | Claude | - |
| DEC-022 | 2026-09-08 | **`guard-adr.mjs` also runs on the `Bash` matcher**, and `guard-commit.mjs` carries an independent Accepted-ADR check against the staged diff | The hook was registered on `Edit\|Write` only, so `sed -i`, a redirect or `cp` rewrote an Accepted ADR with no prompt and no commit-time detection - while the operating default of preferring Bash for edits made that the *normal* path. Two layers because neither is sufficient: the PreToolUse hook only sees commands that name the path, and the commit check only sees what reached the index | Human + Claude | - |
| DEC-023 | 2026-09-08 | **Guard tests must exercise a hook's REACH, not only its parsing** | T14-T19 all sent `tool_name: 'Edit'`, so 19 green tests coexisted with a total bypass. The suite confirmed the guard worked exactly where it worked. New cases T20-T30 send `Bash` payloads and staged-diff scenarios | Claude | - |
| DEC-024 | 2026-09-08 | **`db:verify` runs every script inside a transaction that is always rolled back, and refuses non-dev targets** unless `HRM_VERIFY_ALLOW_NONDEV=i-understand` | The suites are adversarial writers: each run left a fabricated `people.employee.hired` row in the permanently undeletable audit log (4 had accumulated), marked outbox rows processed via an UPDATE with no WHERE, and leaked partition `audit_event_p203007`. The runner honours `PGHOST`/`PGDATABASE`, so aimed at production it would have rewritten production policy history | Claude | - |
| DEC-025 | 2026-09-08 | **Four Must-Know Rule exceptions are named in their ADRs rather than left implicit** - Rule 6 (ADR-0006), Rule 2 (ADR-0008), Rule 12 (ADR-0009), Rule 8 (ADR-0016) | Accepted ADRs outrank `CLAUDE.md` in the authority order, so an ADR that contradicts a Must-Know Rule would *silently repeal it* on acceptance. Each exception is now stated, scoped to the narrowest surface that makes the design work, and explicitly declared non-precedential | Human | - |
| DEC-026 | 2026-09-08 | **ADR-0012 is BLOCKED pending Q10** (build payroll vs buy/integrate), and its `Reconsider when: Never` has been removed | OR-07 records GreytHR's ESS already includes Salary, and the requirements already assume a payroll hand-off. If Q10 resolves to "buy", ADR-0012 should be **retired or voided, not amended** - a rules engine that is never built must not sit Accepted at the top of the authority order. `Never` cannot honestly be recorded while the premise is unresolved | Human | On Q10 |
| DEC-027 | 2026-09-08 | **A fifth Must-Know Rule narrowing: `unconfirmed_fields` may be updated on any effective-dated row, including a closed historical one** | Rule 3 permits closing a period but not mutating a historical one. The badge records how much confidence we have in a value (DEC-020); it cannot change what any date resolves to, and HR confirming a badged default must not require a new policy period. Enforced narrowly - a change touching the badge AND any other column is refused. Missing from DEC-025, which named only four | Claude | - |
| DEC-028 | 2026-09-08 | **Business-date decisions use `fn_business_date()`, never `CURRENT_DATE`** | The server runs UTC and the company runs in Kochi (UTC+05:30), so for 5h30m every day `CURRENT_DATE` is the PREVIOUS business day. Measured during review: db date 2026-09-07 while Kochi was 2026-09-08. The Rule 3 back-dating guard therefore accepted a retroactive closure every night. The function resolves `org_setting['company.timezone']` so the timezone stays configuration (Rule 11), not a literal | Claude | - |
| DEC-029 | 2026-09-08 | **A back-dated effective period requires an explicit opt-in** - `SET LOCAL hrm.allow_backdated_period = 'on'` | Guarding UPDATE and DELETE while leaving INSERT open left history fully rewritable: a back-dated row changes what an already-resolved date returns, demonstrated live. Seeding and genuine backfill remain necessary, so the opt-in makes them deliberate and greppable rather than blocking them outright | Claude | - |
| DEC-030 | 2026-09-08 | **Protective triggers are `ENABLE ALWAYS`, and TRUNCATE is blocked by statement triggers** | Triggers created ENABLE are silently switched off by `session_replication_role='replica'`, a session GUC with no lock and no catalogue trace that `PGOPTIONS` carries into any client including the migration runner. TRUNCATE fires no row trigger at all and is separately grantable, so the 'application role is not the owner' mitigation never covered it | Claude | - |

| DEC-031 | 2026-09-08 | **Track A (manager demo) is superseded by full-product scope**, and the build order is **foundation before breadth** | Human decision this session. The 14-module feature list expands scope past the demo cut line recorded in `CURRENT_SLICE.md`, so that card is retired rather than silently overrun. Foundation first because two of CLAUDE.md's own rails currently block compliant breadth work: `packages/authz/authz-matrix.yaml` does not exist, and Forbidden Actions bar adding a route without a matrix entry; and Must-Know Rule 2 is violated by every write path today - `apps/api/src/` contains zero audit and zero outbox INSERTs, with `audit_event`, `outbox_event` and `audit_column_policy` all holding 0 rows. Adding 8 more modules on that pattern multiplies the violation instead of containing it | Human | - |
| DEC-032 | 2026-09-08 | **The reporting graph is resolved over `employment.manager_id`; `reporting_relationship` and `reporting_closure_current` are NOT created** | `ai/context/rbac-rules.md` names both tables as the source of the graph and neither exists. `employment` already carries the manager edge, already effective-dated, already under the Rule 3 immutability triggers and an EXCLUDE constraint forbidding overlapping periods. A second table holding the same edge would be a second source of truth for the most security-sensitive relation in the system, and the two would drift. `fn_reporting_subtree_asof` walks it recursively, depth-capped at 10 and cycle-guarded by path membership - the guard is necessary because `ck_employment_not_self_managed` blocks only self-management, so A manages B manages A is accepted by the schema today. **`rbac-rules.md` needs a text amendment to stop naming tables that do not exist** | Claude | Before ADR-0005 is accepted |
| DEC-033 | 2026-09-08 | **The employment lifecycle is an append-only log plus an FSM held as data, with `employee.status` demoted to a derived cache** | `employee.status` had four legal values and nothing that moved between them - a field somebody eventually UPDATEs by hand. Three separations: (a) `employment` stays "what your job is" (effective-dated), `employment_event` becomes "what happened" (append-only), because a confirmation is not an assignment and a resignation is not one at all; (b) the legal moves live in `employment_status_transition` and `employment_event` carries a composite FK into it, so an illegal transition is refused by the database rather than by a service method a later code path forgets to call (Rule 11 + "prefer a constraint over a rule"); (c) the triple is STORED on the event and the FK targets the triple, so editing the legal moves later cannot reinterpret recorded history - same reasoning as the snapshotted `attendance_policy_id` in 0012 | Claude | - |
| DEC-034 | 2026-09-08 | **Cached lifecycle columns are DERIVED from the log as of the business date, never patched per-event** | Migration 0014 shipped a per-column CASE that applied every event unconditionally, so a future-dated event took effect the moment it was saved: a pre-boarding hire read `active`, and - the damaging one - a resignation recorded with a future last working day read `exited` immediately, which would have stopped attendance, leave accrual and payroll for somebody still working. Same class as the `valid_to IS NULL` trap in temporal Rule 2, reappearing on the lifecycle log. Found by running 0014, not by reading it. 0015 replaces the mechanism with `fn_refresh_employment_status`, which recomputes every cached column from the log: correct for future events by construction rather than by a condition somebody must remember, self-healing, and one definition instead of one per column per event type | Claude | - |
| DEC-035 | 2026-09-08 | **`marital_status` refused; `blood_group` admitted but badged unconfirmed** | `marital_status` is an ordinary HR field that `security-guidelines.md` already classifies, but it is the precise input Employee Handbook §1.3.4.5 would consume and CLAUDE.md bars implementing that clause in any form - so 0014's verify suite asserts the column is ABSENT (check L24), making reintroduction deliberate rather than casual. `blood_group` is health data with no described emergency-response process, which purpose limitation forbids; it is conventional on Indian HR forms and plausibly belongs beside the emergency contact, so it was classified `SENSITIVE` and flagged rather than silently included or silently refused. Emergency-contact columns are **third-party data**: collected from the employee about somebody who never consented and has no relationship with the company | Human (HR) + Claude | Before Phase 9 |
| DEC-036 | 2026-09-08 | **Employee field selection is an explicit allowlist plus a SUBJECT check, and `hr_admin` is default-denied personal data until the real field mask exists** | `GET /employees/:id` was `SELECT e.*` on an `@Authenticated()` route with no role restriction. Migration 0014 added eleven personal columns and every one began serialising to any logged-in caller - proven by logging in as a plain employee and reading a colleague's record: 35 fields including date of birth, gender and personal phone. `date_of_birth`/`gender`/`personal_phone` were already exposed before 0014; the migration widened it. Nothing failed and no test broke, because the disclosure arrived as a side effect of adding columns to a table - exactly why ADR-0005 and `rbac-rules.md` require a default-deny field registry instead of reviewer attention. The interim mask is an explicit column list plus `me.employeeId === id`, which is a **subject** comparison, not the inline role check Rule 1 forbids. **HR deliberately gets nothing extra**: granting it would require another inline role check, and default-deny is the direction to fail in. Pinned from the outside by `npm run privacy:test` (18 checks), which asserts on the JSON body rather than the SQL, so a future `SELECT *`, a new column or a widened gate all fail | Claude | When packages/authz ships fieldMask |
| DEC-037 | 2026-09-08 | **Redis dev port default moves 56379 -> 55379** | Windows reserves TCP 56291-56390 for Hyper-V dynamic allocation on this machine, so the DEC-009 default of 56379 fails to bind with `An attempt was made to access a socket in a way forbidden by its access permissions` - a message that reads like a permissions problem rather than a reserved range. Checked with `netsh interface ipv4 show excludedportrange protocol=tcp`. 55379 sits below the first reserved band (55891) and beside the working Postgres port 55432. DEC-009 keeps its reasoning; only the number changes | Claude | - |
| DEC-038 | 2026-09-08 | **`js-yaml` added as a root devDependency, for the authz matrix test only** | `packages/authz/authz-matrix.yaml` is the authorization test oracle and something has to parse it. It was already present transitively at 4.3.2, but depending on a transitive package means the suite breaks on an unrelated dependency change - and this is the highest-value suite in the repo, so it must not be the fragile one. Hand-rolling a YAML subset parser was the alternative and was rejected: a parser bug would silently mis-read the oracle, which is worse than a dependency. Test-only, never imported by application code (CLAUDE.md: a dependency needs its reason recorded - OWASP A03:2025) | Claude | - |
| DEC-039 | 2026-09-08 | **`packages/authz` exists: declarative TypeScript policy, `authz-matrix.yaml` as an INDEPENDENT oracle, graph questions behind a port** | ADR-0005 chose declarative TS over an external policy language, so role-to-permission mapping is code (`src/policies.ts`) and not a database table - only grants and graph edges are relational. The matrix YAML is deliberately NOT generated from the policies: a file that restated the code would copy a bug into both and test nothing, so it was written from the requirements and disagreements are treated as findings. Graph questions go through a `GraphPort` the application implements against migration 0014's resolvers, so the package has no driver dependency and the whole policy set is testable against a fake graph (dependencies point inward, per architecture-principles). 35 actions, 6 roles, **210 cells asserted in BOTH directions** plus 13 named threat cases - 308 assertions total | Claude | - |
| DEC-040 | 2026-09-08 | **`scope()` refuses when the actor holds no role the policy names** | Found by the matrix suite, not by review: `can()` correctly denied `finance` on `work.log.read`, yet `scope()` returned a self-only predicate, because the row-filter helper hands "your own rows" to anybody. Not a disclosure on its own - the rows are the caller's - but `can` and `scope` are supposed to be separate concerns, not contradictory ones, and a caller who composed `scope` without `assertCan` would have served a role holding no grant at all. A role-set precondition now aligns the two by construction rather than by the caller remembering. Both fail-closed paths return one shared deny-all predicate that renders as literal `false`, so a caller who forgets to branch on `kind` still gets an empty result set rather than an unfiltered one | Claude | - |
| DEC-041 | 2026-09-08 | **HR reads operational data unconditionally; the `is_ancestor_of_actor` deny-override is scoped to personal detail, lifecycle and employee.update** | `rbac-rules.md`'s deny-override example is specifically about COMPENSATION - "a manager who is also an HR admin still cannot see their own manager's salary". Leave, attendance and work logs are ordinary HR processing, so `hr_admin`/`hr_ops` are unconditional there. Personal detail, lifecycle history and employee updates DO carry the override, so HR cannot read up its own chain of command. The matrix originally marked all of them `conditional`; the generated suite caught the disagreement, which is what an independent oracle is for | Claude | Revisit when compensation tables land |
| DEC-042 | 2026-09-08 | **Authentication now reads roles from `user_role` fresh on every request, and every outcome is audited** | ADR-0010 forbids caching an authorization input across requests, so `fn_user_roles` is called per request rather than baked into the session: a revoked grant or a disabled account takes effect on the NEXT request. `app_user.role` is retained as `Actor.role` purely so unretrofitted controllers keep working (expand/contract); `Actor.roles` is the real thing. Failed logins are counted and locked at 10 for 15 minutes - NIST SP 800-63B r4 permits rate limiting and specifically rejects composition rules and forced rotation. Every outcome, INCLUDING failures, emits an audit row with a distinct reason code while returning an identical 401, so the audit trail distinguishes `no_such_account` from `bad_password` but the response does not. `audit_event` went from 0 application rows to a live security trail - Must-Know Rule 2 satisfied for the identity surface | Claude | - |
| DEC-043 | 2026-09-08 | **Organisation structure splits IDENTITY (mutable) from STRUCTURE (effective-dated), across two period tables rather than one polymorphic one** | `department` and `designation` were flat tables with a code and a name - no parent, no head, no validity, no way to close one. So "effective-dated organisational changes" was true of who reports to whom (0014) and false of everything structural: a department could be re-parented with an UPDATE and last year's cost allocation would silently re-attribute itself. `temporal-data-rules.md` names "a designation's display name" as the canonical mutable reference data, so names stay editable and STRUCTURE moves to `department_period` and `team_period`. Two tables rather than one `org_unit_period` because departments NEST while a team's parent is a department and never another team - the foreign keys state that instead of a comment, and neither table needs a generated discriminator key to hang its EXCLUDE constraint on | Claude | - |
| DEC-044 | 2026-09-08 | **`designation.retired_on` is a DATE, not a boolean and not a delete** | A retired designation must stay valid for the historical `employment` rows referencing it while being refused for a NEW assignment - so the question is "was it retired on the date being assigned", which a flag cannot answer and a delete makes unanswerable. Enforced by `tg_employment_designation_active` on INSERT rather than in a service method, because a migration, a backfill or a later code path would each have to remember the rule independently | Claude | - |
| DEC-045 | 2026-09-08 | **`team_membership` is deliberately NOT an authorization scope graph** | ADR-0005 has exactly two graphs and `rbac-rules.md` is explicit that merging them is how a project lead ends up reading somebody's disciplinary file. Team membership is an organisational grouping for structure and reporting; it confers no access. Consequently `org.team.member.manage` is HR-only rather than delegated to the team's lead - there is no principled way to say "the lead" without inventing a third graph, which would need an ADR. 0017's check **G18** fails if any team function starts reading `user_role`, so the boundary is mechanical rather than remembered | Claude | Needs an ADR if team-based access is ever wanted |
| DEC-046 | 2026-09-08 | **Document content STREAMS THROUGH THE API; no presigned URLs** | A presigned URL is the conventional scalable answer and the wrong one for Tier 1 documents. It is an unrevocable bearer token for the object: an employee offboarded mid-download keeps it, which contradicts ADR-0009's whole argument that revocation must take effect locally and immediately; it produces no audit row per access, so the individual accesses that make a bulk export noticeable never exist; and it ends up in logs - security-guidelines.md bans presigned-URL query strings from logs precisely because they are credentials in a URL, and the surest way to keep them out is not to mint them. The cost is that document bytes traverse the API process, which on a single-VM deployment for a few hundred employees is a non-issue | Claude | If document volume ever makes streaming a bottleneck |
| DEC-047 | 2026-09-08 | **The upload MIME allowlist is a CHECK constraint, not configuration** | Must-Know Rule 11 forbids hardcoding POLICY - rates, thresholds, approval chains. An executable-content allowlist is a security CONTROL, not policy, and a control an administrator can widen from a web form is not a control. SVG is absent deliberately: it is HTML-equivalent, a stored-XSS vector, and has no HR use case. The same list is duplicated in packages/authz/upload-validation.ts for the request path and both must agree - 0018's check D1 asserts the database half excludes SVG, and the upload suite asserts the application half refuses it | Claude | - |
| DEC-048 | 2026-09-08 | **The document TYPE is the unit of access control, and its class travels on the authorization ref** | Document content is unstructured, so fieldMask cannot reach inside a PDF - an offer letter's compensation figure is as invisible to the field registry as its font. document_type.data_class is therefore the whole basis of the decision, and ResourceRef.dataClass carries it into the policy. An ABSENT dataClass is treated as RESTRICTED, so a caller who forgets to pass it gets the narrowest answer rather than the widest - the same failure direction as the default-deny field registry | Claude | - |
| DEC-049 | 2026-09-08 | **A line manager gets NO document access at all** | Everywhere else a manager reads their subtree's operational data. A manager has no business reading a report's Aadhaar, bank proof or medical certificate; document handling is an HR function, and the manager's legitimate interest - is this person at work, did they take leave - is already served by attendance and leave. This is a NARROWING of the reporting graph rather than an exception to it: the graph still decides which rows are reachable, the policy decides who reaches them. Asserted end to end by docs:test step 7 | Claude | - |
| DEC-050 | 2026-09-08 | **In-page previews render from a `blob:` URL; `Content-Disposition: attachment` is NEVER relaxed** | Users need to view documents, not only download them, but serving them inline would let a scripted PDF or a polyglot execute in the origin that holds the session cookie. Rather than weaken the header behind a query flag, the client FETCHES the bytes with JS and builds a `blob:` URL - so the browser never navigates to the API response, the header stays `attachment` for everyone including anybody who pastes the URL, and previews still work. Images render in an `<img>` (which cannot execute anything); PDFs in an `<iframe sandbox>` with NO allow tokens, giving an opaque origin with scripts refused; DOCX/XLSX are honestly labelled download-only rather than shown in a viewer that would fail. A `Content-Security-Policy: default-src 'none'; sandbox` header was added as a belt in case some future change ever does serve inline | Claude | - |
| DEC-051 | 2026-09-08 | **A VIEW is audited distinctly from a DOWNLOAD, via `?mode=view`** | Same bytes, same authorization, same headers - only the audit event type differs (`documents.document.viewed` vs `.downloaded`). "Opened the appraisal in the browser" and "took a copy" are different facts about the same person, and collapsing them loses information an auditor wants. The parameter cannot relax anything: it is read only when choosing the event name | Claude | - |
| DEC-052 | 2026-09-08 | **Document counts on the employee list are gated by the POLICY, not by a role check** | A count of MEDICAL certificates is information about somebody's health even without the files, so it cannot be shown to everyone - but writing `role === 'hr_admin'` in the controller is what Must-Know Rule 1 forbids. Instead `hr.ts` asks `authz.can(ctx, 'documents.document.list', { dataClass: 'PERSONAL' })` with no subject: `isSelfAndNotRestricted` cannot be satisfied without one, so employees and managers get false and hr_ops/hr_admin get true. The answer falls out of the EXISTING documents policy rather than needing a new rule, and the counts are omitted from the RESPONSE rather than hidden in the markup | Claude | - |
| DEC-053 | 2026-09-08 | **Settings retrofitted onto `AuthorizationService`; a manager can no longer read company policy** | `/settings` carried `@Authenticated('hr_admin', 'manager')` while `authz-matrix.yaml` had said `manager: deny` for `config.policy.read` and `config.setting.read` all along. The matrix was right and the route was wrong, and nothing compared the two - which is the whole point of OR-19: a policy nobody enforces is documentation. ADR-0005 amendment (a) makes `org_setting` and the policy tables ORGANISATION-scoped, denied by default and reachable only by an explicit administrative permission, because a policy row is a historical derivation input and reading one is a privilege rather than a default. All five routes now call `assertCan`; `hr_admin` writes, `hr_ops` and `auditor` read, everybody else gets 403. **403 rather than 404 is deliberate here**: policy is organisation-scoped, so a denial reveals no record's existence and the settings screen is not a secret - the 404-hides-existence rule in `rbac-rules.md` is about per-employee records. Pinned by `npm run settings:test` (15 checks), which asserts the ROUTES agree with the matrix rather than trusting they were wired correctly | Claude | - |
| DEC-054 | 2026-09-08 | **Rebranded to ART HRM; gold is brand identity, near-black is the primary action** | The brand gold (#f5c518) behind white text is about 1.7:1 - illegible, and far from WCAG AA's 4.5:1. The brand itself never does this: its logo and banner pair gold with near-black. So gold surfaces carry `ink-900` text (10.4:1) and the primary action became near-black on white (17.6:1), matching the black banner. `brand-700` (#8a6a08) is the bronze end of the ramp and the only gold legible as TEXT on white (5.0:1). Focus rings are near-black, not gold: an indicator needs 3:1 and gold on white manages ~2.2:1. **Second reason, independent of contrast**: this palette already spends amber on "pending / late", so a gold primary action would blur the semantic palette and an amber badge would stop reading as a state. Gold is therefore confined to the logo, the active-nav tint and brand accents. The company name moved through `org_setting`, which is MUTABLE by ADR-0019 - a rename needed no migration and no deployment, which is the point of having built it as configuration | Claude | - |
| DEC-055 | 2026-09-08 | **The ART mark is traced from MEASURED pixels, not eyeballed, and the brand hex values come from decoding the icon** | The supplied icon is a 108x108 lossy raster - byte-identical to the Bing thumbnail - so it was decoded (WIC to PNG, then a hand-rolled PNG inflate in Node) and swept by angle at several radii. That measurement corrected two wrong guesses: the notch is **81 degrees** wide (95 to 176), not the 68 I had estimated, and the needle reaches **r*0.96**, very nearly the rim, rather than stopping short. Flat gold is **#ffca06** - the MODAL pixel value; the mean is #f4c716, dragged down by the original's gloss shading, which a flat UI must not reproduce - and the grey is **#7a7b7e**, cool rather than neutral. The mark is inline SVG rather than the supplied file or its URL: a 108px raster is soft above favicon size, and referencing the Bing URL would put a third-party request on every page load, leak the viewer to Microsoft, and depend on a content-hash cache path that will rot. Geometry is asserted arithmetically (all grey vertices inside the notch and the disc) because no browser is available here to screenshot | Claude | If the official vector is supplied |
| DEC-056 | 2026-09-08 | **The ART mark ships as the supplied RASTER, not a traced vector** | Three attempts to reverse-engineer SVG paths from the 108px icon were all wrong, and the final measurement showed the model could never fit: along a single ray through the centre the mark reads gold-white-grey-white-gold, so the white separation is radially non-monotonic and the notch apex is off-centre. A single wedge cannot express that; the best-fit trace still disagreed with the source on **7% of pixels** - precisely the white separation that gives the mark its shape. The supplied icon is used directly instead, with everything outside the disc made transparent (decoded and re-encoded as RGBA by a hand-rolled PNG encoder, since no image library is available). **For a 28px header mark and a favicon, 108px is ample** - 28px needs 56px at 2x DPI, and vector would only matter above ~54px. The earlier "inline SVG is always better" reasoning was wrong for this size: an exact raster beats an inexact vector. A larger rendering (print, splash, email header) still needs the official vector from whoever holds the brand assets | Claude | If a rendering above ~54px is needed |
| DEC-057 | 2026-09-08 | **`project_member` is effective-dated, closing OR-17 and giving the project authorization graph a time dimension** | `rbac-rules.md` named `project_member` as one of ADR-0005's two scope graphs and asserted it was effective-dated. It was not - `UNIQUE (project_id, employee_id)` and a `created_at`. That left exactly two possible behaviours and both were wrong: leave the row and a contributor rolled off a project keeps access forever; delete it and they lose access to the effort THEY logged while last quarter's project report silently changes. `packages/authz` had to document the gap and ignore the as-of date it was handed. Membership is now a period with an EXCLUDE constraint (so leaving and rejoining is representable, overlapping membership is not), under Rule 3 immutability, and `fn_is_project_member_asof` answers the question the authorization layer was already asking. `DbGraphPort.isProjectMember` and the `projectMembership` scope predicate now both honour the date | Claude | - |
| DEC-058 | 2026-09-08 | **The timesheet review cycle is an FSM-as-data, and CORRECTED/RESUBMITTED are EVENTS rather than states** | `timesheet_period.status` was a bare `text` column with a CHECK - exactly the weakness `employee.status` had before 0014 - so the same fix in the same shape: a transition table as data, a composite FK on the whole triple, and an append-only `timesheet_transition` log. The requirement describes SUBMITTED -> REJECTED -> CORRECTED -> RESUBMITTED; that is modelled as `return`, `correct`, `submit` over five states, because **a resubmitted timesheet is not in a different condition from a submitted one - it IS submitted**, and adding `corrected`/`resubmitted` as states would force every downstream query to know that three values all mean "waiting for a manager". The log is what records that it happened twice. **No self-approval is a CHECK**, with the subject denormalised onto the log and a trigger asserting it still matches its parent - rbac-rules structural-integrity item 1, both halves, because a denormalised column nothing verifies is a constraint on a lie. `approved` is terminal, and the work-log lock was extended to `under_review` (adding a state between submitted and approved without extending the lock would have opened a window to edit effort while a manager read it) while deliberately sparing `returned`, which exists to be edited | Claude | Revisit if the generic workflow engine (Module 10) absorbs it |
| DEC-059 | 2026-09-08 | **CORRECTION: the earlier 185-versus-181 `db:verify` discrepancy was a real test-isolation bug, not a counting artifact** | I attributed it to chaining two verify invocations in one command line. That was wrong. 0012's check N8 borrowed EMP003 and cleared only `attendance_day` - but punches are APPEND-ONLY, and `punch:test` signs in as anu.krishnan, who IS EMP003. Running `db:verify` after `punch:test` left the derivation spanning both sets of punches: 563 minutes instead of 545. The failure then aborted the file, silently costing N9..N12 - and 11 checks in 0012 with N8 eighth means exactly 4 lost, which is the 185 - 181. N8 now creates its own employee, so it has no punch history by construction. Verified order-independent by running verify after demo/punch/docs suites: **207 checks, 0 failures**. Third occurrence of this pattern (0014 L13, 0017 G5), so it is now first in the traps list | Claude | - |
| DEC-060 | 2026-09-08 | **A report is NOT a new permission: each report reuses the resource action it reports on, and the scope predicate is composed INTO the SQL** | The obvious design is `report.leave.read`, `report.attendance.read` and so on. That is a trap: two permission sets covering the same data drift apart, and the day they do a report shows what the detail view refuses - the worst possible direction for the mistake. So the leave report IS `leave.balance.read` applied to many rows, and a report can never reveal more than the equivalent detail screen. The second half matters as much: `rbac-rules.md` warns that filtering in application code "still leaks via counts, pagination totals and timing", and a report is nothing BUT counts and totals, so this is where that warning actually bites - a manager who cannot open E3's record but receives a headcount including them has still learned something about E3. Every handler renders `scope()` into the statement and lets Postgres aggregate AFTER the filter, so no unscoped total ever exists in the process. The 0020 functions are all per-employee for exactly this reason; a function returning a pre-aggregated total would make a scope-correct report impossible to build on | Claude | - |
| DEC-061 | 2026-09-08 | **A report's authorization ref carries the CALLER as its subject: `assertCan` answers "may you read this resource type at all", `scope` answers "whose rows"** | An employee's own attendance report returned **404**. A report is a COLLECTION request with no single subject, but the self-scoped policies are written around `isSelf`, which cannot be satisfied by a ref with no subject at all - so `assertCan` denied before `scope` was ever computed, and the manager's report came back empty for the same reason. Asking with the caller as subject widens nothing: a role that cannot read its OWN attendance is refused outright, `finance` and `auditor` hold no attendance grant and are still refused, and every role that passes gets exactly the rows its scope predicate admits. The index endpoint asks identically, or it would advertise a report that then 404s. This looks like a shortcut and is the opposite: it separates the two questions that were being conflated | Claude | - |
| DEC-062 | 2026-09-08 | **CORRECTION: the documents report is self-scoped, not HR-only. A report must never be MORE restrictive than the screen it summarises** | I wrote the test expecting an employee to be refused `/reports/documents`, and DEC-061's fix "broke" it. The test was wrong, not the code. `documents.document.list` admits an employee to their own documents - that is how `/documents` works - so refusing them the compliance COUNTS for files they can already open would make the report stricter than the detail view, which is the same drift DEC-060 exists to prevent. Verified empirically before changing the expectation: an employee's run returns exactly one row, their own. The replacement checks are stronger than the original - the report returns the caller and nobody else, and reveals no colleague even as a row of zeroes, because a zero row still discloses that the colleague exists and has filed nothing. **A manager likewise sees only themselves here**, while seeing four people's attendance: `documents.document.list` grants a manager `isSelfAndNotRestricted` (DEC-049), so the same caller is scoped differently per resource. That asymmetry is now asserted, with its reason, so nobody "fixes" it for consistency with attendance | Claude | - |
| DEC-063 | 2026-09-08 | **A task answers to BOTH scope graphs, so there are two functions and two cuts - not a third graph and not a merged one** | Every resource so far fell cleanly on one side: a work log is about a PERSON (reporting graph), a project is about WORK (membership graph). A task is the first that is genuinely both - it lives in a project AND is assigned to a person - which looks like the merge DEC-045 warns about, the one that lets a project lead read a disciplinary file. The resolution is that "tasks by assignee" and "tasks by project" are two different QUESTIONS, each already answered by an existing graph: `fn_task_status` is keyed on the assignee and filtered by the reporting graph exactly like a work log, `fn_project_task_status` is keyed on the project and filtered by membership exactly like project effort. **The second function is not optional**: every seeded task had `assignee_employee_id IS NULL`, so the assignee cut alone returned nothing to anybody - including HR - while nine real tasks sat in three projects, and "two open tasks belong to nobody" is precisely the finding a task report exists to surface. An unassigned task has no subject, so the reporting graph LITERALLY cannot answer for it; it is not public either, so membership is the right gate. Visible in the output: Vishnu sees one person and two projects (not MOBL), while HR sees all three projects holding no membership at all | Claude | Needs an ADR if task-level access is ever delegated to a project lead |
| DEC-064 | 2026-09-08 | **A reporting function's subject column must be NAMED exactly as `packages/authz` renders it - tasks are the first resource where that is not `employee_id`** | `GET /reports/tasks` returned 200 for HR and **500 for an employee and a manager**: `column t.assignee_employee_id does not exist`. HR passed because HR's predicate is ALLOW_ALL, which renders as literal `true` and names no column; every SCOPED caller failed. A smoke test against HR alone - the role whose report is most interesting to look at - would have declared the feature working. The cause was a contract between two files that nothing checked: `employeeColumn('task')` is `assignee_employee_id` because that is the column on the `task` TABLE, while 0022's function published `employee_id`, copying the other report functions where the two names coincide. The map is right and the function was wrong - changing the map would have fixed this query and broken every future query against the real table. Fixed forward in 0023 by DROP and CREATE, since replacing a function cannot rename its OUT parameters, and 0022 is checksum-enforced (DEC-011/012). `testing/db/0022_task_reporting.verify.sql` check TK1 now asserts the column NAME from `pg_proc`, which is the check that was missing when this shipped | Claude | - |
| DEC-065 | 2026-09-08 | **The per-project task cut composes `scope()` WITHOUT `assertCan`, and that is safe only because of DEC-040** | `work.project.read` admits an employee `when: isProjectMember`, which needs a `projectId` on the ref - and a collection request has none, so `can` would deny every non-HR caller and the cut would vanish for exactly the project members it exists to serve. Relying on `scope` alone is not a bypass because DEC-040 gave `scope` its own fail-closed precondition: an actor holding no role the policy names gets `DENY_ALL`, which renders as literal `false`. So `finance` and `auditor` get no rows, an employee or manager gets their own projects through the membership predicate, and HR gets ALLOW_ALL - each from the same policy, with no role check in the controller. This is the first place that precondition is load-bearing rather than defensive, which is worth recording: DEC-040 was written as a consistency fix and has now become a design dependency | Claude | - |
| DEC-066 | 2026-09-08 | **The demo seed now assigns tasks and dates, because an empty report makes a vacuous test - and the task block had to move below the memberships** | Every task in the seed was unassigned and undated, so the task report returned nothing and any test over it would have asserted nothing - the trap that has now bitten this repo four times (0014 L13, 0017 G5, 0012 N8, and this). The fixture is chosen so every counter has a reason to be wrong: two tasks OVERDUE, two DUE SOON, two open with NO due date, one **done and twenty days past its due date** (which must NOT count as overdue - a seed without that combination cannot distinguish a correct implementation from one that ignores status), and two left DELIBERATELY UNASSIGNED so DEC-063's project cut has something real to report. Dates are offsets from `fn_business_date()`, never literals, so the fixture stays meaningful as the calendar moves. Moving the block was forced by the database: `fn_task_assignee_is_member` refused the insert outright because the tasks sat above the `project_member` rows they depend on. I had written in the comment that nothing enforced assignee-must-be-a-member; the trigger proved me wrong, which is the right direction for that argument to be settled in | Claude | - |
| DEC-067 | 2026-09-08 | **The `/reports` tab strip is built from `GET /reports`, not from the actor's role** | The obvious implementation is `hasRole(actor, 'hr_admin') && <Tab name="documents"/>`, which is Must-Know Rule 1 - an authorization decision inlined outside `packages/authz` - and it fails in both directions: it hides a report from somebody entitled to a narrowed view of it, and offers one that then 404s. The index endpoint asks the policy engine per report and returns the names that will actually load, so the page renders exactly those, and it asks with the same ref `gate` uses so the two cannot disagree. Verified by calling it as three roles and confirming every offered tab returns 200. The `effort` tab is the one exception and is derived rather than listed, because it shares `work.log.read` with the reconciliation report instead of holding an action of its own | Claude | - |
| DEC-068 | 2026-09-08 | **Navigation moved from a flat top bar to a GROUPED SIDEBAR, and the shell sizes itself** | Twelve destinations were competing for one horizontal row, so "My Profile", "My Work" and "Team Effort" each wrapped onto two lines and the bar became a wall of same-weight words with no structure to scan. Horizontal space is fixed and was already exhausted; vertical space is not, and six more modules are still to come - a wider bar postpones the problem by one release. The four groups (**My records · My team · Organisation · Administration**) state the product's actual shape, which is the same distinction the authorization model draws, so navigation and permissions tell the reader one story instead of two. **No group is called "Manager"**: `domain-glossary.md` bans the unqualified word because a line manager and a project manager resolve through different graphs and it calls the ambiguity "a security bug waiting to happen". **The directory stays "Employees", not "People"** - a Person survives rehire and may hold two employments, so the friendlier label would have been a domain error; the same glossary pass fixed unqualified "manager" in the directory's own subtitle. Two implementation mistakes are recorded because both were invisible until measured: the first sidebar pinned itself with `top-[57px]` and `calc(100vh-57px)` against a header that is actually ~53px, which would have shown a 4px strip of page above it, so the shell is now a flex column that takes whatever height the header takes (`h-dvh`, since `100vh` includes mobile browser chrome); and a left accent bar sat in the row's flow and pushed every label 11px right of its own group heading, so it was dropped - the gold pill already carried that job (DEC-054) and a second indicator adds no information. `min-w-0` on `main` is load-bearing: without it the wide report tables push the page sideways instead of scrolling inside their own container | Claude | - |
| DEC-069 | 2026-09-08 | **`nav:test` checks that every door the shell offers actually opens, and enforces two glossary rules mechanically** | Which links a role is OFFERED is an affordance, not a control - `settings:test` already proves the API refuses a line manager who types the URL - so asserting the list would mostly test a constant. What is NOT constant is the link between a hand-written href and a directory on disk: nothing in TypeScript connects them, so `/timesheets` for `/timesheet` compiles, deploys, and 404s for whoever clicks it. The suite reads the nav out of the source rather than restating it, so it cannot drift into agreeing with itself, and it also pins the two naming rules above plus the two layout mistakes, so reinstating either fails a test instead of a review. **It had to learn to read code rather than prose**: the first run failed both layout checks because the layout's own comments explain the rejected patterns in order to prevent them, so comments are stripped first. And it sets `process.exitCode` rather than calling `process.exit()`, which aborted with a libuv assertion and returned **127 after printing OK** - a suite that reports success and exits non-zero either blocks a gate inexplicably or gets deleted | Claude | - |
| DEC-070 | 2026-09-08 | **Payslips are RECORDS, not a payroll computation - and `payslip.source` keeps that boundary legible** | ADR-0012 (a versioned statutory rules engine) is Proposed and **BLOCKED** on plan question Q10: is payroll built here or bought? So this slice makes no bet on the answer - HR enters a payroll result finalised elsewhere and attaches the PDF that was issued from it. Nothing computes PF, ESI, professional tax or TDS; no rate, threshold or rounding rule is stored, and all three are named in ADR-0012 as still-unresolved amendments. **This is the right thing to build while Q10 is open**, because it works identically whether payroll is eventually built or bought. `payslip.source` is `manual_entry` on every row so that when an engine does land, ADR-0012's requirement to record the resolved rule version per line applies to ITS rows and not to these, and nobody mistakes a hand-typed figure for a rule-derived one. `payslip_component_type.is_statutory` is descriptive for the same reason: it marks what a future engine will own without implying this one calculated it | Claude | When Q10 resolves |
| DEC-071 | 2026-09-08 | **The PDF and the structured figures are required to AGREE, by constraint: `declared_net_minor` is what the PDF says, the lines are the data, and issuing is refused unless they match** | The requirement was that the document and the salary breakdown describe the same payslip. Enforcing that in the service layer would last exactly until the next code path, so it is a database guard: `fn_payslip_totals` derives gross/deductions/net from the lines, and a payslip cannot be issued unless the derived net equals the net HR typed in from the PDF, the document is attached, its scan is clean, its type is `payslip`, and **it belongs to the same employee**. That last one is the check with teeth - without it HR could attach one person's PDF to another's payslip and the product would serve it as authoritative. Verified at ONE PAISE of disagreement (check PS4), because a tolerance is how this kind of invariant dies. Totals are never stored: a stored total that disagreed with its own lines would be a lie the database was helping to tell, so they are derived in one place and `declared_net_minor` is deliberately the only figure captured twice | Claude | - |
| DEC-072 | 2026-09-08 | **The compensation deny-override, which DEC-041 deferred: no one reads the payslip of somebody above them in their own reporting line** | DEC-041 scoped `is_ancestor_of_actor` to personal detail, lifecycle and employee.update with the note "Revisit when compensation tables land". They have landed. `rbac-rules.md` states the rule with a worked example specifically about pay - *"a manager who is also an HR admin still cannot see their own manager's salary"* - and insists it be **true rather than accidentally true**, which is why it is a deny-override and not an omission from the allow list: allow lists are additive, so the `hr_admin` grant would otherwise re-open what the `employee` grant never offered. `isAncestorOfActor` returns false when the subject IS the actor, so HR still reads their own payslip. **A line manager gets nothing at all** - the one resource where the reporting graph does not apply, because a manager needs to know whether somebody was at work, not what they were paid (DEC-049 denied them documents on the same reasoning). **hr_ops is denied too**, a departure from every other HR resource: DEC-041 made HR unconditional for leave, attendance and work as "ordinary HR processing", and pay is not, which is what the `finance` role exists for. `auditor` is denied the read because its legitimate need is the TRAIL, and `fn_audit_payslip` records who read whose payslip with no amount at all | Claude | Segregation of duties - see OR-28 |
| DEC-073 | 2026-09-08 | **FOUND: `ALTER DEFAULT PRIVILEGES` silently defeats Tier 1 isolation for every new compensation table** | `rbac-rules.md` structural integrity item 5 requires compensation tables behind a separate DB role "so a SQL injection in the leave module physically cannot read salary". Migration 0008 line 93 runs `ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT ON TABLES TO hrm_app`, so **every new table grants SELECT to `hrm_app` automatically** - the role that serves the leave module would have been able to read the salary register, and nothing would have said so. Tier 1 isolation is therefore opt-OUT, which is the wrong default for the most sensitive class of table in the system. 0024 REVOKEs explicitly on all five payslip tables and creates `hrm_payroll` with the grants instead; check **PS20** enumerates the tables and asserts `hrm_app` holds nothing, so the next compensation table cannot inherit the default quietly. **PS21** additionally asserts no payslip function is SECURITY DEFINER, which would hand back exactly the access the REVOKE removes. The isolation is structural but NOT YET EFFECTIVE, because the API still connects as the owner - see OR-29 | Claude | - |
| DEC-074 | 2026-09-08 | **A payslip is VOIDED, never deleted - and voiding withdraws its document, so no reachable orphan PDF is left behind** | The requirement was that a deleted or invalid payslip must not leave an accessible orphan document. A DELETE cannot satisfy that: it takes the reason with it and leaves the object in MinIO with no record of who was ever allowed to read it. So `tg_payslip_no_delete` refuses deletion outright, void requires a reason, and the void transition withdraws the `employee_document` row in the same statement - which makes the guarantee structural rather than something the API must remember on every read path. The bytes survive (they are evidence, and 0018 keeps versions append-only) but every path that joins through `employee_document` stops returning them, proven for HR and for the subject in `payslip:test` step 15. Voiding is also what frees the period: the exclusion constraint admits one LIVE payslip per employee per period, so re-drawing a corrected payslip is the ordinary reason to void one | Claude | - |
| DEC-075 | 2026-09-08 | **FIXED: the payslip writer flag leaked, and one leaked GUC undid four separate controls** | `payslip.status` is meant to move only through `payslip_event`, enforced by a trigger reading a transaction-local flag the event guard sets around its own UPDATE. 0024 set the flag and never cleared it - and `SET LOCAL` is scoped to the TRANSACTION, not the function (a function's own `SET` clause restores only the parameter it names, here `search_path`). So after ONE legitimate transition, `UPDATE payslip SET status = 'draft'` was accepted for the rest of the transaction, and the FSM, the composite FK onto the transition table, the no-self-issue CHECK and the issue-time reconciliation could all be walked straight past. **The blast radius was wider than the column**: with the status forged back to `draft`, both freeze triggers correctly concluded the payslip was still a draft, so an ISSUED payslip's line amounts became editable (breaking the correspondence the issue guard had just established) and its `document_id` became re-pointable at another employee's PDF. Both tested as broken and neither trigger was at fault. Fixed in 0025 by clearing the flag immediately after each UPDATE - the shape migration 0007 already uses for the leave ledger, whose check V6 exists because this trap was found once before in this repo and was not applied here. Found by running the invariants, not by reading them | Claude | - |
| DEC-076 | 2026-09-08 | **The payslip record is the authority for its PDF, so there are two doors to the same bytes on purpose** | The `payslip` document type is RESTRICTED, which in the documents module means HR-only: `isSelfAndNotRestricted` fails for the subject, so an employee cannot reach their payslip PDF through `/documents` at all. Their door is `GET /payslips/:id/document`, gated by `payroll.payslip.read`, which admits the subject because **a wage slip is something the person paid is entitled to see**. Two doors with different rules would normally be the drift DEC-060 warns about, and the reason this is safe is that the payroll door is narrower in every respect EXCEPT adding the subject themselves - which is never a disclosure to a third party. It also sidesteps OR-25 (an employee cannot see their own RESTRICTED documents) for the one RESTRICTED type where the subject's right of access is not in doubt. The upload is narrowed to `application/pdf` although the document allowlist is wider, which removes the polyglot-image surface entirely for this type; and `self_uploadable` is false because an employee producing their own payslip is the fraud that prevents | Claude | - |
| DEC-077 | 2026-09-08 | **Money crosses every boundary as integer paise in a STRING, and is parsed with BigInt text arithmetic - never `parseFloat(x) * 100`** | Rule 4 bans floating point for money. The obvious implementation of a rupee input is `Math.round(parseFloat(s) * 100)`, which is a Rule 4 violation wearing a rounding call: the multiply happens in binary floating point, so the value being rounded is already wrong (0.07 * 100 is 7.000000000000001) and the error only shows when an amount lands on the wrong side of a half. `toPaise` in `payroll.ts` splits the digits as TEXT and assembles them with BigInt, so `20000` and `20000.00` are provably the same value and `.5` is fifty paise rather than five. The same discipline runs outward: amounts are `::text` in every query, asserted as strings by the test, and `formatPaise` in the UI inserts the decimal point into the integer rather than dividing - so the figure on screen is the integer from the database, and the running total in HR's form agrees with what the database will derive to the last paise | Claude | - |
| DEC-078 | 2026-09-08 | **CORRECTION: two of my own fixtures were wrong, and the checks that caught them were right** | First: 0018's check **D15** refused the whole verify run because the seeded object keys were `seed/payslip-emp001-2026-06.pdf` - an employee number in an object key, which is exactly what D15 forbids, since "bucket listings, backups and access logs all travel differently from the database". My own seed comment claimed the keys carried no personal data while the code put it there. Keys are now `seed/<document uuid>/<version uuid>.pdf`, mirroring `objectKeyFor` in the request path. Second: the seed's MinIO step tried to UPDATE `sha256_hex` after uploading and was refused by `fn_document_version_guard` - 0018 check D9 makes a version's key, hash and size immutable. A hash can only be known after the bytes exist, so the seed now does what demo.sql already does six times over: the OWNER disables the rail explicitly, writes, and puts it back. Both were the database being right and my fixture being wrong, and both were found by a check rather than by review | Claude | - |
| DEC-079 | 2026-09-08 | **FIXED: 0019's checks W14 and W15 were SILENTLY SKIPPING, and silent coverage loss is worse than a failure** | Both borrowed a seeded draft timesheet and returned an INFO notice when none existed. Running `demo:test` first - it SUBMITS the seeded draft - left no draft, so both printed INFO instead of PASS. Nothing failed; `db:verify` simply reported 230 checks where the run before reported 232, and the discrepancy only surfaced because the totals were being counted. A failure is loud and this was not. They now build their own two employees and their own timesheet period, so they are order-independent by construction. **Fifth occurrence of the borrowed-fixture trap** in this repo (0014 L13, 0017 G5, 0012 N8, the task seed) and the first where the symptom was a missing check rather than a wrong one - which is why the count is now recorded in `CURRENT_SLICE.md` and re-checked after the app suites run | Claude | - |
| DEC-080 | 2026-09-08 | **A refused duplicate payslip is a 400, not a 500 - and the test that accepted `!ok` was the reason it survived** | The exclusion constraint admitting one live payslip per employee per period surfaced as `500 Internal server error`, which reads as a broken product rather than a refused duplicate and tells HR nothing about what to do next. `create` now maps the constraint names to actionable messages. The reason it shipped that way is worth recording: my own assertion was `check('a duplicate is refused', !dup.ok)`, which passes on any non-2xx including a 500. The assertion now names the status exactly and requires the message to be actionable - a negative test that accepts every kind of failure cannot tell a correct refusal from a crash | Claude | - |
| DEC-081 | 2026-09-09 | **The punch card is built around three STATES, and in each one the hero is whatever actually matters** | The card gave its 30px hero to the elapsed TIME and put the only available action beside it as an ordinary `md` button - so before checking in, a meaningless **`0h 0m`** was the focal point of a screen whose entire purpose is one press. Three separate blocks of prose (the header hint, the retention paragraph, the office list) then out-measured both, and an empty "no punches yet" panel spent a third of the height saying nothing. Now: **NOT STARTED** makes the action the hero (no zero, no empty punch list, one line of context), **ON THE CLOCK** makes the live elapsed time the hero with a pulsing dot, **DONE** makes the day's total the hero and offers checking in again quietly. The panel is tinted per state so it is readable before any text is - gold for the pending action (DEC-054 confines gold to brand accents and reserves amber for pending/late, and this is a call to action rather than a warning), emerald while on the clock. `Button` gained an `lg` size for this: a hero action at `md` competes with the text beside it instead of leading it. The note field moved behind an "Add a note" link - it is used on a small minority of punches, and an empty text box beside the primary action reads as something you must fill in first | Claude | - |
| DEC-082 | 2026-09-09 | **The consent line stays beside the button; only the retention detail moved into a disclosure** | Cutting prose was most of the point, and the obvious cut was the four-line "what is stored" paragraph plus the office list. But location is captured AT the punch, so the sentence saying that has to be legible **before** the action rather than one click away from it - collapsing all of it would have traded clutter for a real reduction in transparency. So one short line sits under the button, inside the same panel, and the retention rules and geofence list live behind a native `<details>` - which is the answer to "tell me more", a different question. Native `<details>` rather than React state: it works with no JavaScript and is keyboard-operable for free | Claude | - |
| DEC-083 | 2026-09-09 | **FOUND: the seed fabricated a DERIVED attendance verdict for today, and the dashboard truthfully rendered three contradictory facts** | The card showed **"8h 15m · In 09:12 · Out 18:30"** above **"No punches yet"** beside a **"Check in"** button. Every one of those was a correct reading of the data: `attendance_day` said the day was finished, `attendance_punch` was empty, and `checkedIn` was false. The data was the lie. `attendance_day` is a DERIVATION - `fn_derive_attendance_day` computes it from punches - and the seed's `generate_series(..., fn_business_date())` wrote a row for the CURRENT business date with plausible in/out times and no punches behind it. The series now ends at **yesterday**, which keeps the month's history (the summary still has something to show) and leaves today genuinely open - also the better demo, since the first thing a visitor can do is punch in and watch the verdict derive itself. Guarded by new check **N13**, scoped deliberately to the business date: historical seeded days legitimately have no punches, because the attendance history predates the punch log and back-filling fake punches for it would be a worse fiction than the summary it supports | Claude | - |
| DEC-084 | 2026-09-09 | **FIXED: my own state discriminator repeated the same incoherence one layer up - a derived TOTAL is the wrong thing to ask about whether something happened** | The redesigned card tested `done = !checkedIn && day && worked_minutes > 0`. Checking in and out inside the same minute produces `worked_minutes: 0` with two punches on the record, so the card fell through to **"Ready to start your day"** for somebody who had just finished - the same class of contradiction DEC-083 had just been fixed for. The discriminator is now the punch LOG (`punches.length > 0 || day !== null`), because the punches are the facts and the total is a summary of them, and a summary is allowed to be zero. `day` is included because a future attendance regularization can produce a finished day with no punches behind it. Found by driving the three states through the API rather than by reading the branch | Claude | - |
| DEC-085 | 2026-09-09 | **FIXED: 0012's N9 and 0016's I11 were order-dependent, and the trap was recorded a full session before either was fixed** | **N9** borrowed EMP004 and cleared only `attendance_day`. Punches are APPEND-ONLY, so a single manual check-in at 00:15 while testing the dashboard survived the DELETE, the derivation read it, `first_in` became 00:15 instead of 09:16, and the day came back `present` on entirely correct code - then aborted the file and took N10..N13 with it. **This is DEC-059 verbatim**: N8 hit the identical trap, and N8 alone was fixed while the neighbour sharing its weakness was left in place. Fixing the instance rather than the pattern is how a trap reaches its sixth occurrence. **I11** was worse in a quieter way: `SELECT id FROM user_role WHERE valid_to IS NULL LIMIT 1` with no ORDER BY takes whichever row the planner reaches first, which moves as the table grows and as earlier checks in the same file insert their own grants - the third appearance of that exact shape after 0017's G14. Both now own their fixtures, and I11 additionally asserts the **forward close** as well as the refused edit: proving only that an in-place edit fails would pass equally against a trigger that refused every UPDATE, which would break the close-then-supersede path Rule 3 exists to permit. Verified by running `db:verify` after each of punch/demo/payslip/docs/reports - **259 checks after every one** | Claude | - |
| DEC-086 | 2026-09-09 | **A test must not degrade the demo fixture: `payslip:test` moved off the seeded months** | The suite worked in June, May and March while the seed issues payslips for June, July and August - so its idempotency reset voided two SEEDED payslips on every run, `issued_payslips` fell from 6 to 4, and nothing said so. Being idempotent about its own leftovers is not the same as being harmless to everything else. The suite now lives in January to April, which the seed never uses, so the reset can only reach payslips an earlier run of the suite created - and it asserts explicitly that all three of the target employee's seeded payslips are still issued, so a future collision fails a check instead of quietly eroding the demo | Claude | - |
| DEC-087 | 2026-09-09 | **The three masters are shaped by the data model, not by CRUD - a rename and a move are different acts** | A generated CRUD screen would have offered one "Edit department" form and been wrong. Identity is mutable (DEC-043): a department that changes its name is the same department, so renaming is a PATCH that takes effect everywhere at once. But where a department SITS is effective-dated, so re-parenting closes the placement in force and opens a new one - it needs a date and a reason, and last quarter's cost allocation keeps resolving to last quarter's parent. Hence two endpoints and two visibly different forms. The same reasoning runs through the other two: a designation is RETIRED with a date and stays on the list (DEC-044), showing how many people hold it before you retire it, because retiring a title 40 people hold is a different act from retiring an empty one; and an employee TRANSFER is the same close-and-open as a department move, so the UI says out loud that the previous assignment is kept rather than replaced. Creating an employee writes THREE rows in one transaction - `employee`, `employment` and the `joined` lifecycle event - because the event is the part that is easy to omit, impossible to reconstruct, and the only thing that moves the status off `pre_boarding` | Claude | - |
| DEC-088 | 2026-09-09 | **FOUND: a department CYCLE was creatable, and its cost was not an error but a silently understated headcount** | 0017 forbade a department being its own parent, and check G9 proved the traversal TERMINATES on a cycle. Neither of those says a cycle cannot EXIST - and it could, through exactly the path the new master uses: forward-close a placement, then re-parent under a descendant. Reproduced before writing the UI that would have been the first thing able to reach it. **The failure is silent**: `fn_department_subtree_asof` stops when it re-meets a department, so it returned 1 row where 2 existed, and `fn_department_headcount_asof` rolls that walk up into HR's reports - producing a headcount that is quietly too low in a report that looks entirely normal. Migration 0026 adds a trigger refusing a parent already inside the department's own subtree AS OF `valid_from` (a future-dated placement's legality depends on the structure in force when it takes effect, not today). A trigger rather than a CHECK because the question needs a traversal, and in the database rather than the controller because a migration or a backfill would each have to remember it. Checks G19/G20 pin the two-step and three-step loops; **G21 pins that a legitimate re-parent still works**, because refusing every re-parent would satisfy G19 and G20 while making the master useless | Claude | - |
| DEC-089 | 2026-09-09 | **OR-18 CLOSED: personal data now flows through the policy and the field registry, and the WRITE list is derived from the READ mask** | DEC-036 took an interim default-deny - `isSelf ? personal : null` - when `packages/authz` had no field mask, and said in terms "revisit when fieldMask ships". It has. HR now reads the address, date of birth and phone it needs, decided by `people.employee.personal.read` and masked by the registry, with no role compared in a controller. **The registry is doing real work rather than rubber-stamping**: `emergency_contact_*` and `blood_group` are SELF_ONLY, so HR still cannot read them - the emergency contact is third-party data about somebody who never consented, and the blood group is health data with no stated purpose (DEC-035, OR-16) - and a line manager still gets nothing at all. The edit endpoint then derives its writable set from that same mask, so a caller who may not READ a field is refused when they try to SET it. That is one rule instead of two lists that would eventually disagree, and it means an employee can set their own emergency contact while HR cannot set it for them. Reading somebody else's personal record emits an audit row, which is the `audit_read` obligation the policy already attached to this action | Claude | - |
| DEC-090 | 2026-09-09 | **`org.designation.manage` is a separate action from `org.unit.manage`** | It would have been free to reuse `org.unit.manage` and it would have been wrong. That action's own matrix note explains itself as being about REORGANISING - re-parenting a department, which is why a department head must not hold it. A designation is a job-title catalogue: nothing nests, nothing has a head, and the risk differs in kind, because retiring one closes it to every FUTURE hire organisation-wide while every historical `employment` row keeps pointing at it. Same cells today, different blast radius, and separable the first time somebody wants to delegate one without the other. `designation` was added to `ResourceType`, which forced a decision on its subject column - `null`, because a designation is a title in a catalogue and not a fact about a person | Claude | - |
| DEC-091 | 2026-09-09 | **FOUND: a client must NEVER compute the business date - my own test failed at 00:35 IST for exactly the reason `fn_business_date()` exists** | The masters suite used `new Date().toISOString().slice(0, 10)` for "today" and failed five checks. At 00:35 IST the UTC date is the PREVIOUS day, so every effective date it sent was yesterday in business terms and the effective-dated rails correctly refused it as back-dating. This is precisely the mistake the domain glossary names - "the IST offset makes UTC truncation wrong before 05:30" - and the reason `business_date` is computed at ingest and stored rather than derived from a timestamp. **A client that computes its own today is wrong for five and a half hours out of every twenty-four, which is the window nobody tests in.** The suite now takes the business date from the API. The second failure was adjacent and produced a real improvement: closing a placement on the same day it started leaves a period covering no days, which the empty-range CHECK refuses - it now gets its own sentence instead of a generic one | Claude | - |
| DEC-092 | 2026-09-09 | **CORRECTION: `profile-privacy` asserted "hr_admin sees no personal fields" and was deliberately inverted - replaced with something stronger** | That check pinned DEC-036's interim posture, so closing OR-18 broke it. Weakening a privacy assertion is exactly the kind of edit that should be hard to make quietly, so it was not simply deleted: what replaces it asserts that HR reads the fields HR administration needs, that the four SELF_ONLY fields are STILL unreachable, and that HR cannot SET them either - three checks where there was one, and they fail if somebody later widens the registry by accident. The suite went from 18 to 20 checks in the course of relaxing one of them | Claude | - |
| DEC-093 | 2026-09-09 | **0017's G9 became a BACKSTOP check: it now plants a cycle past the guard rather than creating one through the front door** | G9 existed to prove the traversal terminates on a cycle, and it built its fixture through the ordinary INSERT path. 0026 closes that path, so the check had to force one in with the rail explicitly disabled - and it is worth keeping for exactly that reason. The walk must still be safe on data the guard never saw: a restore or bulk load performed by the OWNER with triggers off (the residual power the migration headers concede, and the reason the application must never connect as the owner), or rows written before 0026 in a database upgraded rather than rebuilt. The depth cap and the path-membership test are what make that true and no other check exercises them | Claude | - |
| DEC-094 | 2026-09-09 | **Production stack published on `127.0.0.1:4787` behind the host nginx**, mirroring the shape already used by `hr-agent` on 4767 | The VM already runs several apps behind one nginx that terminates TLS on :7777 and path-routes to loopback ports. 4765/4766/4767 are taken. Publishing anywhere but loopback would put an employee-data app on the LAN with no TLS in front of it | Claude | - |
| DEC-095 | 2026-09-09 | **The edge nginx config is a whole `nginx.conf`, not a `conf.d` snippet** | `limit_req_zone` and `map` are only valid in the `http{}` context, and rate limiting is one of the three things this layer exists to provide (with security headers and the body cap). A snippet cannot express it | Claude | - |
| DEC-096 | 2026-09-09 | **Rate limits are keyed on a realip-corrected `$remote_addr`, not on `$http_x_forwarded_for`** | The host nginx APPENDS to whatever XFF the client sent, so a limit keyed on the raw header is evaded by varying it. `set_real_ip_from` over the private ranges plus `real_ip_recursive on` yields the rightmost untrusted address, which the client cannot forge | Claude | - |
| DEC-097 | 2026-09-09 | **Deploy health check expects `401` from `/api/auth/me`**, not `200` from a health route | There is no unauthenticated health endpoint, and adding one would need an `authz-matrix.yaml` entry (a Forbidden Action without it). An authentication refusal proves the process is listening and routing; a 502 proves it is not, and a 500 proves it is up and broken. Three probes are checked, not one - `/healthz` (edge), `/api/auth/me` (API), `/login` (web) - because "the port answers" has hidden a broken app before | Claude | - |
| DEC-098 | 2026-09-09 | **Redis and the BullMQ worker are deliberately ABSENT from the production stack**, though ADR-0013 names both | No code connects to either. An unused Redis is an unused password and an unused listener. ADR-0010 also makes Redis authoritative for the session record, which the API does not do yet (sessions are in PostgreSQL), so adding Redis now would imply a session migration as well. Both land with the worker that needs them | Claude | - |
| DEC-099 | 2026-09-09 | **The session cookie's `Secure` flag is env-gated (`HRM_COOKIE_SECURE`), defaulting to false** | ADR-0010 requires Secure and the code carried it as a TODO awaiting TLS; this deployment supplies the TLS. It cannot be unconditional because local development is plain HTTP, where a Secure cookie is simply not stored and nobody can log in. The production compose sets it true. The `__Host-` prefix remains outstanding - it is a rename that logs every user out, so it belongs in its own change | Claude | - |
| DEC-100 | 2026-09-09 | **The public path prefix is a BUILD-time value (`NEXT_PUBLIC_BASE_PATH`), applied through `apps/web/lib/base-path.ts`** | Next resolves `basePath`/`assetPrefix` during the build, so no runtime variable can change it - the same constraint `hr-agent` handles with `VITE_BASE`. Next covers `<Link>`, `router.push` and `next/image`, but NOT root-absolute strings the app builds itself (`fetch('/api/...')`, a raw `<img src>`, `window.location.href`); those seven call sites now go through `withBasePath()`. Default empty, so a root deployment and every existing test are byte-identical to before | Claude | - |
| DEC-101 | 2026-09-09 | **Seeding is not containerised** | `scripts/seed.mjs` needs psql AND the MinIO client from node_modules, while the migration image deliberately carries neither (it is built on `postgres:18-alpine` with nodejs only). More importantly the seed DELETES and recreates its data, so making it one command away from a live stack is the wrong ergonomics. A real first-administrator bootstrap does not exist yet and is on the go-live list | Claude | - |
| DEC-102 | 2026-09-09 | **The edge nginx config is an envsubst TEMPLATE (`nginx.conf.template`), substituting one variable, `HRM_BASE_PATH`** | FOUND BY RUNNING IT. With `basePath=/panasa-hrm` the browser asks for `/panasa-hrm/api/...`, which `location /api/` does not match - so every API call fell through to `location /` and Next proxied it onward. The failure is SILENT (the app still works, slowly) and it puts a Node hop in front of every document stream, which is exactly what routing /api at the edge exists to avoid. `proxy_pass http://hrm_api/api/` strips the prefix, and with an empty base path the same form collapses to the identity, so one template serves both deployments | Claude | - |
| DEC-103 | 2026-09-09 | **`HRM_API_ORIGIN` is a BUILD arg for the web image, not a runtime variable** | FOUND BY RUNNING IT. Next evaluates `next.config.ts` `rewrites()` during `next build` and serialises the result into `routes-manifest.json`, so the runtime env var is inert - the container logged `ECONNREFUSED 127.0.0.1:4000` while the API was answering 401 perfectly well on `api:4000`. The compose file's comment claiming the runtime value protected the fallback was simply wrong. Both the arg and the env var are now set, to the same value, so they cannot drift apart unnoticed | Claude | - |
| DEC-104 | 2026-09-09 | **`proxy_read_timeout` lives at the server level, not in `hrm_proxy_params`** | nginx refuses a duplicate directive in one context, so a location that included the shared params could not then raise its own read timeout - which `/api/` needs for document streaming. Setting it on the server and overriding per location is legal, because those are different contexts. nginx crash-looped on this until it was fixed | Claude | - |
| DEC-105 | 2026-09-09 | **Merge `aaef373` reconciled onto ONE deployment stack: the 4787-behind-the-host-nginx topology, with the other branch's better pieces folded in** | Two complete deployment stacks were merged and the CONFLICT MARKERS WERE COMMITTED - `apps/api/src/auth.ts` would not compile. The two were not variants, they were different topologies: this one publishes `127.0.0.1:4787` plain HTTP behind the shared host nginx that already fronts /hr-agent/ and friends; the other bound host :80/:443 and terminated its own TLS, which assumes PanasaHRM owns the VM. The 4787 model was kept because that is the actual host arrangement. Adopted from the other branch: the API image's workspace-scoped reinstall (979 MB -> 292 MB), psql-in-a-node-image for the migration runner (515 MB -> 266 MB), the profiled destructive `seed` service, the pinned MinIO release, the owner/application role split, and scram-sha-256. Its duplicate Dockerfiles, nginx config and runbook were removed rather than left to be followed by mistake; `docs/runbooks/deployment.md` is now a pointer | Claude | - |
| DEC-106 | 2026-09-09 | **Production PostgreSQL stays on `timezone=UTC`; the merged-in `TZ: Asia/Kolkata` was REVERTED** | Its stated reason - that the server timezone must match `fn_business_date()` or a punch lands on the wrong day - is factually wrong. `fn_business_date()` is `now() AT TIME ZONE COALESCE(org_setting 'company.timezone', 'Asia/Kolkata')`, which converts an absolute timestamptz and is therefore immune to the server setting; the function's own COMMENT states that the server runs UTC. Setting the server to IST would have left the function correct while making production silently disagree with `docker-compose.dev.yml` for anything that does touch CURRENT_DATE - a divergence that only shows up between 18:30 and 00:00 IST | Claude | - |
| DEC-107 | 2026-09-09 | **One switch, `HRM_SECURE_COOKIES`, drives both the cookie's `Secure` flag and its `__Host-` prefix; the competing `HRM_COOKIE_SECURE` I had added was deleted** | The merge left BOTH, plus two `secure:` keys in one object literal where the last silently won. The dangerous combination was real: `HRM_SECURE_COOKIES=true` alone produced `__Host-hrm_session` WITHOUT `Secure`, which a browser refuses to store outright - nobody could have logged in, and the cause would have looked like a session bug. One variable cannot produce that state, which is the argument for deriving both from it. Verified end to end: login now returns `__Host-hrm_session; HttpOnly; Secure; SameSite=Lax` | Claude | - |
| DEC-108 | 2026-09-09 | **The environment reference is `env.example`, not `.env.example` - and `.gitignore` line 15 contradicts the governance rules** | `CLAUDE.md` lists writing to `.env*` under Forbidden Actions and `.claude/settings.json` hard-denies `Write(**/.env.*)`, because that filename pattern is where real secrets live and a guard with an exception is not a guard. But `.gitignore` carries `!.env.example`, which explicitly expects a tracked file by that name - and `apps/api/src/auth.ts` referred readers to one that did not exist. The content was written to `env.example` (no leading dot, so it trips neither the deny rule nor the ignore pattern) and the dangling reference was repointed. **This needs a human decision, not a workaround:** either drop `!.env.example` from `.gitignore`, or carve `.env.example` out of the deny list and `git mv` the file. Nothing in it is secret - every value is a local default already committed in `docker-compose.dev.yml` or `seed.mjs` | Claude | Next session |
| DEC-109 | 2026-09-09 | **The published port moved to `127.0.0.1:4788`, superseding the 4787 named in DEC-094 and DEC-105** | 4787 was picked as the next free slot after 4765/4766/4767, but it was found already bound during verification (on the development machine, by the editor). The stack was therefore proven on 4788 and the configuration said 4787 - a gap between what was tested and what would deploy, which is the kind of difference that only shows up at `docker compose up` on the VM. Now aligned: `HRM_PUBLISH_PORT=4788` everywhere, and `deploy.sh` reads the value back out of `prod.env` so its health probes cannot drift from what compose publishes. DEC-094 and DEC-105 are left intact as the record of the original choice and of the merge reconciliation; only the port number in them is out of date, not the reasoning | Claude | - |
| DEC-110 | 2026-09-09 | **`deploy.sh`'s health probe drops its `|| echo 000` fallback - curl already prints `000` itself** | `curl -w '%{http_code}'` emits `000` when it gets no response, so the fallback appended a SECOND value to curl's own: a healthy probe returned `200000`, never matched `200`, and the script reported failure and exited 1 while all six containers were healthy and serving. Found by running `./deploy.sh` end to end for the first time - every earlier verification had driven compose directly and hand-rolled the probes, which is exactly the gap that let it survive | Claude | - |
| DEC-111 | 2026-09-09 | **The seed's demo-password guard moved from a compose `${VAR:?}` into the container's own command** | **Compose interpolates the WHOLE file on every command - `build` and `up` included - and a `profiles:` entry does NOT exempt a service from that.** So `${HRM_DEMO_PASSWORD:?...}` on the profiled `seed` service aborted every ordinary deploy with an error naming a variable the operator had been correctly told to leave blank: `./deploy.sh` on the VM died at `==> Building images`. The guard now runs inside the seed container, so it fires only when the seed is actually invoked - which is all it was ever meant to do. **Why the verification missed it:** every local run substituted a demo password into the env file, so the blank case - the normal case for a real deployment - was never exercised. Now tested both ways: deploy succeeds with it blank, and the seed still refuses | Claude | - |
| DEC-112 | 2026-09-09 | **The `basePath` redirect loop is fixed in the APP with `skipTrailingSlashRedirect: true`, NOT by changing the host nginx - my own fix was rejected in favour of the one already pushed** | Two people diagnosed `ERR_TOO_MANY_REDIRECTS` independently and agreed on the mechanism: with `basePath` set, Next 308s `/panasa-hrm/` to `/panasa-hrm`, while the host nginx 301s the other way, and nginx's redirect is absolute so it points back at the same host:port and nothing breaks the cycle. **We proposed different fixes, and mine was the worse one.** I rewrote `DEPLOY.md` §3 to drop the slash-adding `return 301` and use a prefix location - which works, and which I verified end to end - but that snippet is the house convention for EVERY app on that shared VM (`NGINX-DEPLOY-GUIDE.md` step 2), so it asks the host to make an exception for one app. `skipTrailingSlashRedirect` is Next's documented answer for precisely this case - a proxy in front already owns the trailing-slash decision, so the framework must not also have one - and it leaves the shared config untouched. §3 is therefore restored to the house form, with the reason the app tolerates it. **What my reproduction did contribute is the negative result**: three candidate host configs measured against a real `NEXT_PUBLIC_BASE_PATH` build, showing the documented one looped to curl's `--max-redirs` cap, a prefix location returned 200 in one hop, and a `proxy_pass` with a trailing slash 404'd everything - the last being a genuinely different fault that the troubleshooting section now separates by status code, because the two configs differ by one character. Also worth keeping: the application has no middleware, no `redirects`, no `trailingSlash` and no server-side `redirect()`, and neither nginx emits a redirect near this path, so that single 308 was the only redirect available to pair with the host's - which is why the fix belongs in the app. **And the process lesson stands, twice over:** §3 had never been executed end to end, which made it a hypothesis shipped to another team as an instruction; then the diagnostic command I added to fix it was itself broken by a heredoc eating its line continuations, caught only by reading the diff before committing. The version in the file was extracted back out of `DEPLOY.md` and run verbatim | Claude | - |
| DEC-113 | 2026-09-09 | **The work-log entry stores project, task and sub-task - and DERIVES the sub-project rather than storing it** | The requested hierarchy is Project -> Sub-project -> Task -> Sub-task, and the obvious shape puts all four identifiers on `work_log_entry`. That reintroduces the exact problem 0027 exists to close, one level up: a fourth column free to disagree with the other three, needing a third constraint to keep it honest. **A sub-project is not an independent choice - it is a property of the task**, so given the task both the sub-project and the project are determined. The entry therefore stores what is actually chosen and the sub-project is recovered by joining, through `v_work_hierarchy`. An incoherent sub-project becomes unrepresentable instead of merely rejected. The UI still cascades all four levels, because that is how a person narrows a long list - the cascade is navigation, not four facts to persist. Check WH12 fails if somebody adds the column later without also adding the constraint that would make it safe | Claude | - |
| DEC-114 | 2026-09-09 | **The pre-existing integrity gap 0027 closed matters more than the two levels it added** | `work_log_entry.project_id` and `task_id` were two INDEPENDENT foreign keys and nothing had ever required them to agree, so an entry could name project CPRT with a task belonging to MOBL. `POST /work-log` inserted both straight through without checking. Every effort report groups by project, so one such row silently moves somebody's hours onto the wrong client's total - **nothing raises, the report just disagrees with reality**. Closed with a composite foreign key rather than a controller check, because the requirement is that arbitrary ids cannot be combined at all: a check answers for one code path, a composite FK answers for the next controller, a bulk import and a hand-run UPDATE. Verified clean first - 37 entries, 0 mismatches - so this was closing a door, not repairing damage | Claude | - |
| DEC-115 | 2026-09-09 | **HR recording effort for an employee is a SEPARATE action (`work.log.write_for`), not a widening of `work.log.write`** | "HR cannot add work logs" was reported as a UI problem and was two backend ones. `POST /work-log` had **no employee parameter at all** - every insert used `me.employeeId`, so the endpoint was structurally incapable of recording a log for anybody else whatever the caller's role; and `GET /projects` filtered by project MEMBERSHIP, so HR, a member of nothing, received an empty dropdown. Neither would have been found by testing the form. The fix is a new action on the ORGANISATION graph rather than a wider `work.log.write`: that action is graph `self`, and `self` is what stops an employee writing into a colleague's timesheet, so every cell in its matrix row depends on it staying narrow. `manager` is DENY on the new action on purpose - authoring a timesheet and approving it are different acts, and an approval of something you wrote yourself is not an approval | Claude | - |
| DEC-116 | 2026-09-09 | **Provenance (0028) is recorded on the row, and the migration could not backfill it** | Once HR can record effort on somebody's behalf, "8 hours on CPRT" stops being self-evidently something the employee said about their own day. `entry_source` and `entered_by_employee_id` make the difference visible to an approver rather than leaving it in an audit table somebody would have to think to consult. **FOUND BY RUNNING IT:** the first version backfilled with an UPDATE and failed - 0019's ENABLE ALWAYS period lock refuses any write touching an entry inside an approved period, and it has no bypass GUC, unlike `hrm.allow_backdated_period`. That absence is deliberate (ADR-0015/0016 correct history by adjustment, never in place) and **a migration is not an exception to it** - adding a bypass so one migration could write would have weakened the rail permanently. `entry_source` is therefore added with a DDL default, which row triggers never see, and `entered_by_employee_id` keeps NULL on history with the trigger requiring it on insert | Claude | - |
| DEC-117 | 2026-09-09 | **Bilingual support is an in-repo dictionary, not `next-intl`; Arabic uses Latin digits** | There was NO i18n of any kind - `lang="en"` hardcoded, no library, every string inline across 16 routes. A library was considered and rejected on fit rather than on principle: `next-intl` earns its keep on locale-segmented ROUTING and server-component message loading, and this application is 19 client components behind one shell with no locale in the URL, so its useful surface here is a keyed lookup - which is also a dependency to record under the supply-chain rule for no capability the dictionary lacks. **The type IS the missing-key test:** `ar` is annotated with a type derived from `en`, so an omitted or invented key is a COMPILE error rather than "attendance.title" appearing on a screen. Arabic renders `ar-AE-u-nu-latn` - direction flips, digits do not - because an HR screen sits beside payroll exports and bank files in Latin digits and a net pay reading ٦٢٨٠٠ beside a statement reading 62800 invites a transcription error. **A native speaker should review the Arabic before it is shown outside the team**, and a real browser pass on the RTL layout is still outstanding - there is no browser in this toolchain | Claude | - |
| DEC-118 | 2026-09-09 | **A passing `next build` is not evidence that a server/client boundary is right** | `isLocale` and `LOCALE_COOKIE` were first exported from the i18n barrel, which carries `'use client'`, and the root layout - a SERVER component - called `isLocale()` on the cookie. It compiled cleanly and then **500'd every page in the application** with "Attempted to call isLocale() from the server but isLocale is on the client". Both now live in `dictionary.ts`, which has no `'use client'`, and the barrel re-exports them. The generalisable part is the verification gap, not the fix: three consecutive builds passed while every route was broken, and it took one HTTP request to find. Checks L1-L4 in `i18n:test` are that request, kept | Claude | - |
| DEC-119 | 2026-09-09 | **Three screens opened on HARDCODED DATE LITERALS, found while adding the period filters** | `attendance` had `useState('2026-09')`, `work` had `useState('2026-09-08')` and `team` had `useState('2026-08-31')`. Each would have shown a fixed month, day or week forever: by October the attendance screen would have opened on September and the work form would have offered to log effort five weeks in the past, **with no error anywhere to notice**. All three now start empty and take the resolved period from the server's own `fn_business_date()`, which is also the only correct source - a browser computing its own "today" is wrong for five and a half hours out of every twenty-four (DEC-091). The month and day arrows were kept, because stepping through history is the commonest thing anybody does on these screens, but they now move an explicit range rather than owning a second piece of state that could disagree with it | Claude | - |
| DEC-120 | 2026-09-09 | **Team effort decided row scope with a ROLE LITERAL INSIDE SQL - a Rule 1 violation, and wrong in substance as well as placement** | `($3 = 'hr_admin' OR em.manager_id = $1)` compared a role name in the WHERE clause of `/team/effort`. Beyond breaking Must-Know Rule 1, it hardcoded the reporting graph as ONE LEVEL DEEP - a manager saw their direct reports and nobody beneath them, disagreeing with every other reporting-scoped screen in the system - and a role added later would have got a silently empty screen instead of a denial. Now `assertCan` decides admission and the composed scope predicate decides rows, exactly as `reports.ts` does. Every filter is ANDed with that predicate, never substituted for it, so `?employeeId=` naming somebody out of scope returns nothing rather than their effort - check T5. Part of OR-19; `work.ts` had four such endpoints and this closes one | Claude | - |
| DEC-121 | 2026-09-09 | **Master data is RETIRED, never deleted - and the master screen is the one place that shows retired records** | Work logs reference projects, tasks and sub-tasks, and a timesheet from March must keep displaying the label it was filed against, so there is no delete control anywhere on `/masters/work-management` and the foreign keys refuse to delete anything referenced (no ON DELETE CASCADE anywhere on the hierarchy - check WH10). Retiring only affects what may be chosen for NEW effort. Two consequences that are easy to get backwards and are both tested: the master list must SHOW inactive records, or they could never be reactivated (M15), while `GET /projects` must HIDE them, or a retired branch stays selectable (M18). A `usage` count sits beside each node so somebody sees, before clicking, that retiring it changes what a live project can log against | Claude | - |
| DEC-122 | 2026-09-09 | **The business date now rides `/auth/me`, and FOUR screens were computing their own with `new Date().toISOString()` - which is the UTC date, a day behind for five and a half hours out of every twenty-four** | Three hardcoded date literals were reported; the sweep found **five** ('2026-09', '2026-09-08', '2026-08-31' plus leave's '2026-09-14'/'2026-09-16' and timesheet's '2026-09-07'), and four screens doing something worse. `new Date().toISOString().slice(0, 10)` looks like today and is the UTC date, so before 05:30 IST it is yesterday - and it was not cosmetic: it set `min` on the effective-from of a DEPARTMENT MOVE, so the form permitted a back-dated effective period (Rule 3); it ended a PAYSLIP's pay period a day short against 0024's declared-net reconciliation invariant; it cut the current day out of every default REPORT window and picked the previous year on 1 January; and it read length of service a month short on the 1st. A dead `todayIsoFallback` helper in `lib/api.ts` was deleted too - unused, but it is what invited four copies of the expression. `fn_business_date()` resolves through `org_setting 'company.timezone'` - **verified by setting the company timezone to Pacific/Midway in a rolled-back transaction and watching the business date become 2026-09-08 while UTC read 2026-09-09** - and now rides `/auth/me`, which every screen in the shell already fetches, so there is one authoritative answer and no new endpoint. It crosses the driver boundary as `::text`, respecting the oid-1082 parser fix in db.ts | Claude | - |
| DEC-123 | 2026-09-09 | **Arabic is complete across all 29 web files, with 17 documented exemptions - and the exemptions are enumerated in the test rather than pattern-matched** | 473 strings were untranslated after the first pass. All of them are now keyed except a list that is written out in `i18n:test` so nothing can hide behind a loose rule: the BRAND WORDMARK ('ART', 'ART HRM' - a logo set in type, whose accessible name IS translated), FORM PLACEHOLDERS showing the shape of a value ('ENG', 'EMP006', 'meera.nair@panasatech.com', '0.00', '1 to 20'), a DOCUMENT REFERENCE ('ADR-0005', useless if a reader cannot then find it) and the DEMO PEOPLE's names, which are seed data rendered from a constant rather than UI copy. Two more categories were reverted after the sweep substituted them: TYPE LITERAL UNIONS (`type Tab = 'headcount' | 'attendance' | ...`) and a `kind` prop carrying an API value - both broke the build, which is the good outcome. Arabic renders `ar-AE-u-nu-latn`: direction flips, digits do not, because an HR screen sits beside payroll exports in Latin digits | Claude | - |
| DEC-124 | 2026-09-09 | **A literal-text substitution pass must require JSX POSITION, and the first version did not - it rewrote identifiers** | The converter replaced an English literal anywhere it appeared, so `function ApprovalsPage()` became `function {t('appr.title')}Page()`, `interface Timesheet` became `interface {t('ts.title')}`, and `attendanceToday` became `attendance{t('dash.today')}`. Four files stopped parsing at once - twice, because the first repair script died on its own regex before reaching the fix. JSX text is bounded by `>` and `<` and a prop value by `="`; an identifier is neither, so requiring those boundaries is what separates markup from a name that happens to contain the same word. Module-level constants were the other half of the same lesson: `TABS`, `ATT_FIELDS` and two `EVENT_LABEL` maps all had `t()` calls inserted where no hook can bind, and all four now hold `MessageKey` values resolved at render. **Every one of these was caught by the compiler, which is the argument for keeping the dictionary typed rather than string-keyed** | Claude | - |
| DEC-125 | 2026-09-09 | **Real-browser verification is driven over the DevTools Protocol using the Edge that Windows already ships - no browser stack was installed** | Playwright is named in the stack description, is not installed, and installing it purely to satisfy a test requirement was ruled out. Edge 152 is on the machine and Node 24 has a global `WebSocket`, so `testing/demo/browser-verify.mjs` launches it headless with `--remote-debugging-port` and speaks CDP directly: nothing added to `package.json`. This reaches what no HTTP suite could - the shell renders its navigation only after `/auth/me` resolves, so `curl` returns the loading state in both languages, which is exactly why the rendered Arabic nav and the RTL layout were unverifiable before. It now asserts computed `direction`, real bounding boxes (the first table column sits to the RIGHT of the last in Arabic), a form's Arabic labels, an inline dialog inheriting rtl, and the language switch through the real `<select>`. **Three of its own checks were wrong first**: it drove a dead port and reported thirty feature failures (a preflight now distinguishes that), it required `#wk-project` on a screen where HR correctly sees an empty state, and - the worst - B22 clicked "any Arabic button", hit تسجيل الخروج, landed on the login page whose two inputs satisfied the assertion, and destroyed the session for every check after it. A test that cannot tell a working dialog from a working sign-out button is not evidence | Claude | - |
| DEC-126 | 2026-09-09 | **The assistant ships as TOOL CALLING, not text-to-SQL - the framing the request arrived with** | Model-authored SQL was designed in full before being rejected, so this is a costed decision rather than a preference. Making it safe needs a least-privilege role with no `USAGE` on `public`, per-request temp tables materialised through `scope()`, `SET LOCAL ROLE` under `BEGIN READ ONLY`, `REVOKE EXECUTE ON pg_catalog.set_config FROM PUBLIC` (a global grant change), a statement validator, a Postgres behaviour spike, and a formal amendment to `security-guidelines.md:25`, which bans string-concatenated SQL and lints for it. All of that is required whether SQL is the primary path or a fallback - a half-safe escape hatch is worse than none, and ADR-0016 already set the precedent that if the narrowly-granted role is not created the feature does not ship. Against that cost: `gpt-4o-mini` authoring SQL against a 28-migration schema it has never seen is the least reliable component in the design, while tool selection is what the model is actually good at. The expensive option was also the less accurate one. The design is preserved verbatim under ADR-0020 *Reconsider when*, and the trigger to revisit is data rather than opinion - every turn matching no tool is logged with `refusal_code = 'no_tool'` and its question text | Claude | When the `no_tool` log shows sustained unmet demand |
| DEC-127 | 2026-09-09 | **Tool selection is a two-stage router: pick a domain, then pick a tool within it** | Flat-listing ~54 tools to a small model measurably degrades which one it picks, and the failure is silent - a confidently wrong answer, not an error. The router costs one extra call of roughly 250 tokens and keeps the second-stage choice under about fifteen tools. It is NOT a security control and must not be described as one: authorization is resolved independently on execution, so a mis-routed question yields a wrong answer and never an unauthorized one. Whether the router survives is an empirical question, so it is settled by `assistant:accuracy` rather than by taste - if the eval shows the router is what loses answers, the fallback is a flat list for actors whose authz-filtered catalogue is already small, which is most employees | Claude | After the first accuracy run |
| DEC-128 | 2026-09-09 | **The module is named `assistant`, though ADR-0014 reserved the name `ai` for this seam** | ADR-0014 seam 3 reserved `ai` for exactly this module. The name is not used because `ai/` at the repository root is the development-time Claude Code harness - context files, agent definitions, the registry - referenced constantly in `CLAUDE.md`. An `ai` module in `apps/api/src` alongside an `ai/` directory of governance documents would make every future grep ambiguous, and the two are opposites: one is how this repo is built, the other is what it ships. `assistant` is the seam ADR-0014 described; only the label differs, and ADR-0020 records that | Claude | - |
| DEC-129 | 2026-09-09 | **The model API key is read from `HRM_LLM_API_KEY_FILE`, with a plain env var accepted for local development only** | The request was that the API key go in env. `security-guidelines.md:87` says the opposite in terms: file-backed Docker secrets on tmpfs, mode 0400, with a `_FILE` convention, and never plain environment variables, never a build arg because they persist in image layers. Both are honoured rather than one overriding the other: `HRM_LLM_API_KEY_FILE` is the production path and `HRM_LLM_API_KEY` works locally, where the alternative is a developer inventing something worse. The code logs a warning naming the file convention when the plain variable is used, so the shortcut stays visible rather than becoming silently normal. The key never reaches a log line and is never a build arg | Claude | Before go-live |
| DEC-130 | 2026-09-09 | **Assistant transcripts are kept 90 days, then DELETED rather than anonymised** | Ninety days is long enough to accumulate the `no_tool` backlog DEC-126 depends on, and short enough that a chat log is not a standing disclosure risk. Deletion rather than anonymisation, because a transcript stripped of its question is telemetry, and the telemetry columns carry no subject data and can simply be retained if anyone wants them - so anonymising here would mean keeping the shell of a record for no reason. **This is an engineering default badged unconfirmed in the DEC-020 sense, and like every other retention figure in `data-inventory.md` nothing enforces it yet** - there is no job. Recording the number makes it reviewable in a way that leaving it open would not | Human (HR) + Claude | Before go-live |
| DEC-131 | 2026-09-09 | **Leave entitlement is answered from the asker OWN BALANCE, never from the leave policy table** | The obvious implementation of "how many casual leaves am I entitled to" is a policy lookup, and it would have been a quiet authorization widening. ADR-0005 amendment (a) makes `leave_policy`, `attendance_policy` and `leave_type` organisation-scoped: denied by default, reachable only through an explicit administrative permission, because a policy row is a historical derivation input and reading it is a privilege. DEC-053 is the proof this matters - settings let a MANAGER read company policy while the matrix said deny, and nothing noticed until the two were compared. So `leave_types_and_rules` and `attendance_policy` are HR-scoped tools, entitlement questions route to `leave_balance.accrued` which is self-scoped and already permitted, and the red-team asserts an employee asking the policy question receives the balance answer and never a policy row | Claude | - |
| DEC-132 | 2026-09-09 | **A composite tool authorises PER SECTION, and a section the actor fails is omitted rather than refusing the whole answer** | `xd_employee_360` spans profile, leave, attendance, effort and timesheet, each with a different action and a different scope predicate. Refusing the whole question because one component is out of reach would make the tool useless to everybody but `hr_admin`; returning the failed section unfiltered would be a breach. Omission is the third option and it is what the dashboard already does - the API simply does not send org-wide figures to an employee, and the screen renders whatever arrived. Each section runs its own `assertCan` and composes its own `scope()`, and the join happens in SQL inside one transaction rather than in JavaScript, because `rbac-rules.md` warns that application-side filtering still leaks via counts, pagination totals and timing - which is precisely what a composite is made of | Claude | - |
| DEC-133 | 2026-09-09 | **No assistant table has a column capable of holding a result row, and a verify check enumerates the columns to prove it** | Storing answers is the obvious feature - conversation history surviving a refresh - and it is refused. It would create a second copy of leave, attendance and work data in a table with different retention, no field masking and different access rules; and replaying a stored answer would disclose rows to a user whose roles had since been revoked, which is exactly what ADR-0010 and DEC-042 exist to prevent, since ADR-0010 forbids caching an authorization input across requests. The transcript therefore keeps the question, the routing decision, the tool, its arguments and counters - enough to debug and to build the `no_tool` backlog - and the rendered rows live only in the browser tab that asked for them. The check is a column enumeration rather than a review note because absence is the kind of property that decays silently | Claude | - |
| DEC-134 | 2026-09-09 | **The OpenAI call uses `fetch` against the wire format; the `openai` package is NOT added** | CLAUDE.md lists adding a dependency without recording why under Forbidden Actions, citing supply chain as OWASP A03:2025. The SDK would buy retry helpers and typed responses for one endpoint this code calls three times per turn with a fixed request shape. This repository already talks to Postgres and MinIO without an SDK ceremony, and `testing/demo/browser-verify.mjs` drives a real browser over CDP rather than installing Playwright (DEC-125). The wire format for chat completions with tools is stable and small. A configurable `HRM_LLM_BASE_URL` also makes an Azure OpenAI or gateway deployment a config change rather than a second adapter | Claude | - |
| DEC-135 | 2026-09-09 | **A mis-selected tool is a QUALITY defect, not a security defect - and the two are gated by separate suites** | This is the property that makes a large catalogue safe, so it is written down rather than left implicit. Every tool re-runs `assertCan` on execution and composes `scope()` into its own SQL, regardless of why it was called, so the worst outcome of an adversarial or ambiguous question is a wrong answer. Conflating the two would be expensive in both directions: it would let a paraphrase miss block a release, and - far worse - it would invite someone to treat a good accuracy score as evidence of authorization correctness. `assistant:redteam` is therefore CRITICAL in the `severity-vocabulary.md` sense at 100% with no waiver, while `assistant:accuracy` is a quality threshold waivable with a DEC entry | Claude | - |
| DEC-136 | 2026-09-09 | **The "subject is the caller" probe that makes collection reads work is INVERTED for an approve-shaped action, and it fails SILENTLY** | `permittedTools` decides whether to offer a tool by calling `assertCan` with the caller as the subject. That is DEC-061 and it is necessary: a policy written around `isSelf` cannot be satisfied by a ref carrying no subject, which is how an employee's own attendance report once returned 404. But `leave.request.approve` carries `isSelf` as a **deny-override** - nobody approves their own leave - so the probe asks the one question that policy is guaranteed to refuse, and `leave_pending_approvals` was never offered to anybody, a manager included. **The symptom was a tool quietly missing from a list, which is indistinguishable from the filter working correctly.** It was found by printing the catalogue per seeded role and counting, not by a failing test - the same lesson as DEC-079, where two verify checks silently skipped and only the total showed it. The scope predicate is wrong for this purpose too: `reportingRows(..., maxDepth: 1)` renders as "self OR direct reports", so composing it would put the manager's own submitted requests into their own approval queue. Rendering the override by hand as `AND r.employee_id <> $me` would work and is a second copy of an authorization rule, which is what ADR-0005 exists to prevent. The tool is therefore **not built in wave 1**; the correct fixes (probe with a real direct report, or give `scope()` an approver-shaped predicate that excludes the actor) are authorization changes and need their own matrix cells. Nothing is lost operationally - `/leave` already shows a manager their queue, and the assistant is read-only so it could never have approved anything | Claude | With the next assistant slice |
| DEC-137 | 2026-09-09 | **An assistant answer is masked as a LIST even when it returns one row** | `fieldMask`'s `neverInList` flag exists to stop bulk exfiltration through a collection response, and it is set on exactly the fields that matter - date of birth, home address, personal phone, exit reason. An assistant answer is collection-shaped whatever its size: it is machine-composed, it lands in a panel and it gets copied. Passing `inList: false` for a single-row result would have made "ask for one person by name, read their address" the documented way around the flag, and the assistant makes that a one-sentence operation rather than a deliberate one. So `maskList` is used unconditionally. The cost is real and accepted: an employee asking about themselves does not get their own date of birth back from the assistant, and must open their profile. Being wrong in this direction costs a missing column | Claude | - |
| DEC-138 | 2026-09-09 | **Derived aggregate and computed columns had to be registered in the field registry, and the registry is where the assistant's exclusions became structural** | Seven resource types - `leave_request`, `leave_balance`, `attendance_day`, `attendance_punch`, `work_log`, `task`, `project` - were entirely unregistered, so `fieldMask` returned an empty set and `maskRow` over any of them produced `{}`. Nothing was broken, because the endpoints serving those screens build explicit column lists by hand rather than masking: the default-deny registry was not protecting them, it was simply absent. The assistant is the first caller that masks them. Two consequences worth recording. First, DERIVED columns need entries too - `fn_attendance_summary`'s `present_days`/`late_days`/`worked_minutes`, the reporting-chain tool's computed `level` - because the mask drops what it does not know, and a summary tool would otherwise return rows of empty objects with no error anywhere. Second, and the reason this is worth the ceremony: **the exclusions stop being promises.** `attendance_punch.latitude`/`longitude`/`accuracy_m`/`distance_m` are absent from the registry, so no tool can return them even by writing them into its own SELECT list, and `leave_request.reason` is SELF_ONLY so the author reads back their own and nobody else does. A comment saying "we do not select this" decays; an unregistered column does not | Claude | - |
| DEC-140 | 2026-09-09 | **The assistant's answer is written FROM THE MASKED ROWS, reversing ADR-0020 §3's "no row of employee data is sent to the model"** | The shipped assistant could not answer a question. Asked "how much casual leave do I have left?" it replied *"the lookup for your casual leave balance returned 2 rows of results"* and put the number in a column off the right edge of a scrolling panel — because §3 forbade the model from seeing a value, so the sentence could only ever introduce a table. That was a genuine privacy property, not an oversight: it made prompt injection over stored data **impossible by construction** and reduced the DPDP cross-border transfer to the sentence the user typed. **The product owner was shown both options and the trade in full, and chose to send the rows.** What is given up: injection is now *contained* rather than impossible, and the transfer is now leave balances, attendance days and work records rather than only a question. What bounds it: the model receives **exactly the array the browser is being shown in the same turn** — `scope()` in SQL then `maskList`, never a pre-mask or re-queried set — so the marginal disclosure is to the provider and never between two users of this system; the answer call carries **no tools**, so a value that reads like an instruction can corrupt a sentence and nothing else; the table is streamed from Postgres **before** the call and is never derived from the model's output, so a bad answer sits beside correct rows; values are fenced, control-stripped and capped at 50 rows / 160 chars / 8000 chars. No authorization property changes and the red-team gate is unaffected — it drives no model — but it gained a section that asserts the **payload** against the same reach sets and banned-column list as the rows, because a caller that quietly widened what goes into the prompt would otherwise break no existing assertion. `HRM_LLM_ANSWER_FROM_ROWS=false` restores the pre-amendment behaviour exactly, without a code change, which is the lever if OR-03's still-unnamed legal owner says no | Human (product owner) + Claude | Before go-live, and immediately if a legal owner is named for OR-03 |
| DEC-141 | 2026-09-09 | **The assistant panel shows prose only — the result table is no longer rendered — and the answer is streamed** | Three changes asked for together, and one of them has a cost worth recording. **(a) No table.** The panel now shows the model's sentences and nothing else. DEC-140's third containment bound was that the table reached the browser before the model call and was never derived from its output, so *"a manipulated or hallucinated sentence sits beside the true rows rather than replacing them"* — **that check is now gone for a real user**, and what remains is prose whose only evidence is itself. The two stronger bounds survive untouched: the model still sees exactly the masked array the asker is entitled to, and the answer call still carries no tools, so an injected value can corrupt a sentence and nothing else. The API is deliberately **unchanged**: the `rows` event still goes first and still carries the columns and values, so restoring the check is a rendering change rather than a protocol one, and the red-team's section 8 still has something to assert. The web component drops the values on arrival rather than holding state it does not draw. The honest consequence is that `assistant:accuracy` — **still an unwritten file** — is now the only instrument that could catch a confidently wrong answer. **(b) Streaming.** `llm.ts` gained an `onDelta` path (`stream: true` plus `stream_options.include_usage`, so token counters do not silently become zero) and the fragments are forwarded as `token` events on the existing SSE stream. ADR-0020's Rule 12 conditions are preserved rather than reinterpreted: the abort timer is still a budget for the **whole read**, not for the first byte, and the single retry is **suppressed once a fragment has been emitted**, because replaying text the user has already read would make the answer stutter. A stream that breaks mid-sentence appends an ellipsis rather than failing the turn or silently presenting half an answer as whole. Only the answer call streams — route and select return a word or a function name nobody watches, and streaming a tool call would mean reassembling `arguments` across chunks for nothing. **(c) Fixed panel height.** `max-h-` became `h-`: a panel sized to its content grew as each turn landed, which read as the window jumping and moved the input out from under the cursor. **`HRM_LLM_ANSWER_FROM_ROWS=false` is now a degraded mode, not an equivalent one** — it restores DEC-140's privacy posture but, with no table beneath it, leaves the user a row count instead of an answer; the string it produces says so rather than pretending otherwise | Human (product owner) + Claude | With the accuracy suite, which this makes load-bearing |
| DEC-142 | 2026-09-09 | **The pay block fires on INTENT, not on the topic; and naming somebody out of reach is a refusal rather than an empty result** | Two over-broad behaviours, found by asking the assistant ordinary questions. **(a) "When is my salary credited?" was refused as a forbidden purpose**, because the pattern matched the bare word `salary`. That is a payment-DATE question — process, not compensation — and it got the full "will not feed a decision about hiring, promotion, discipline or termination" refusal, which reads as an accusation. Worse, it filed a fair question as `forbidden_purpose`, so it never reached the `no_tool` log that ADR-0020 relies on to decide what to build next; over-blocking does not just annoy, it destroys the signal. The list now splits AMOUNT-OR-RECORD from SCHEDULE-OR-PROCESS: `payslip`, `ctc`, `bonus`, `increment`, `compensation` and `appraisal rating` name a figure whatever the sentence says and stay a flat block, while `salary`/`pay` block only when the question wants a figure, a structure, or somebody else's — with a lookahead so "what date is salary credited" survives while "what is my salary" does not. **Nothing is loosened**: there is no payslip tool and no payroll-calendar tool, so a pay question that gets through meets an empty catalogue; ADR-0020 §6 ("the catalogue contains no payslip entity") is the control, and this list was only ever a way to give a clear no instead of a confusing one. 21 must-block and 6 must-not-block cases are now in the red team, in both directions. **(b) An employee asking "what is the leave balance of Priya?" was told "nothing matched that."** `whoClause` ANDs onto the scope predicate, so an unreachable person returns zero rows — right for the query, misleading as an answer, because it reads as a statement about Priya rather than about the asker's permissions. When a named lookup comes back empty, the subject is now resolved through `people.employee.read` (its own `assertCan` + `scope()`, never a free lookup) and `can()` is asked about that subject for the tool's own action; if denied the answer is "Priya Menon is outside what your account can see." **This is not an enumeration oracle, and that is the only reason it is allowed**: `people.employee.read` is scope ALLOW_ALL by policy — *"the directory is PUBLIC_INTERNAL; the FIELD MASK is what keeps personal data out of it, not the row filter"* — so every employee can already find Priya in `/employees`. An ambiguous fragment, an unknown name, and the caller themselves all return `null` and leave the ordinary empty path alone, so the check can only turn a misleading answer into an accurate one, never invent a refusal. The same check runs in `/run`, which keeps that endpoint the identical path and lets the red team prove the refusal with no model in the loop. **(c) Every refusal message was cut to one or two sentences** — the panel shows prose only since DEC-141, so a four-sentence refusal is most of the screen. The two phrases the red team asserts (`fixed limit`, `not a permissions problem`) are preserved deliberately. **Verified**: `assistant:redteam` 827/827 against a spare API on :4001, which needs no key because nothing in that suite drives a model — the base URL is now overridable with `HRM_API_BASE` for exactly that | Human (product owner) + Claude | — |
| DEC-143 | 2026-09-09 | **A SELF-ONLY tool class, masked with `inList: false` — narrowing DEC-137 rather than reversing it** | "What is my phone number?" answered *"nothing matched"*, and "my date of birth" answered *"nothing matched."* Both are on file, both are visible to the asker on their own profile screen (`profile-privacy.test.mjs` asserts "own date of birth is visible"), and the assistant was reporting a MASKING decision as an ABSENCE — the same class of wrong as the out-of-reach case in DEC-142. Two causes: `me_profile` never selected those columns, and DEC-137's blanket `inList: true` would have stripped them anyway, since `personal_phone`, `personal_email`, `date_of_birth` and the address fields are all `neverInList` in the registry. **DEC-137 is narrowed, not overturned.** Its reasoning — an assistant answer is collection-shaped, so treating a one-row result as a detail view would make *"ask for one person by name, read their address"* a one-sentence operation — is about tools that can be POINTED at somebody. A tool marked `selfOnly` cannot be: it declares no argument naming a person, and its SQL filters `e.id = ctx.employeeId` **on top of** `scope()` rather than instead of it (the DEC-120 trap). The controller adds a second, independent guarantee that does not depend on any tool getting its WHERE clause right: a row whose subject is not the caller is **dropped** before masking, so a mistake degrades to an empty answer rather than an unmasked read of somebody else. Only then is `inList: false` applied. The first such tool is `me_contact_details`. ADR-0020 §1 is satisfied exactly — *"can never reveal more than the equivalent detail screen"* — because it reaches precisely as far as the asker's own profile and no further. **Deliberately excluded**: `emergency_contact_*` (third-party data, no notice mechanism) and `blood_group` (health data, unconfirmed purpose, OR-16) — both are open questions in `data-inventory.md`, and an open question is not answered by shipping a tool that reads it. `date_of_birth` is cast `::text` in SQL, because letting the driver return a `Date` puts a timezone on a date-only value (CLAUDE.md rule 5). **Verified**: red team 874/874, including a new section 6c that asserts, per role, that the tool answers for the caller and that handing it another person's number and name **does not move the answer** — the one property the relaxation rests on, and the one a future edit adding a who-argument would silently break | Human (product owner) + Claude | — |
| DEC-144 | 2026-09-09 | **A question naming nobody means THE ASKER, not "everyone in scope" — the default subject is now explicit per tool** | An hr_admin asked the assistant for her leave balance and was told 8 days of casual leave. She has 12; the employee EMP001 has 8. `whoClause` added **no person filter** when nothing was named, leaving `scope()` to decide the set — which for an employee is themselves, so "my leave balance" appeared to work, and for an hr_admin is the whole organisation, so ten rows came back and the model narrated the FIRST as "your balance". A manager got eight rows and the same wrong answer. **This was never an authorization failure** — every row was one the asker may read — which is exactly why 800 passing red-team assertions did not notice: sections 1–5 all check that nothing FORBIDDEN comes back, and nothing forbidden did. It is a wrong answer about the asker themselves, and only the wide-scope roles could produce it, so it was invisible in every employee-account demo. `whoClause` now takes the caller and filters `alias.id = $n` when neither `employeeNumber` nor `nameQuery` is given — still an AND on top of `scope()`, never a substitute (the DEC-120 trap). Tools whose no-argument answer genuinely spans the scope (`leave_who_is_off`, `leave_balance_team`, `attendance_team_today`, `leave_liability`, and the person-less configuration tools) declare `subjectDefault: 'scope'` and call the explicitly-named `whoClauseAnyone`. **The default is `'asker'` so that forgetting to think about this fails towards too little rather than too much**, and the flag is published on `/assistant/capabilities` so the red team reads each tool's intended subject from the system rather than a hardcoded table — the same anti-rot argument that already applies to `action` and `resource`. A third flag, `relatedPeople`, marks a tool whose rows ARE other people by design (`me_reporting_chain` returns the line above you, so it contains colleagues and need not contain the asker at all); for those the suite asserts instead that the answer **differs between askers**, which is precisely the property this bug destroyed — every role was served identical rows. **Verified**: red team 956/956 with a new section 6d, plus a role matrix showing employee → own only, manager → self and reports, hr_admin → everyone, and each role's no-argument call returning their own record | Human (product owner) + Claude | — |
| DEC-145 | 2026-09-10 | **First slice from the tool backlog: four `people`/org tools, with the access question re-derived per tool rather than taken from the backlog markers** | `people_directory_lookup`, `people_department_roster`, `org_department_tree`, `org_teams` — the catalogue goes 24 → 28 and `people` stops being an empty domain. Each tool's access was checked against `policies.ts`, `authz-matrix.yaml` and the field registry **before** it was written, and the answers are not uniform: `people.employee.list` is allowed to employee/manager/hr_admin/hr_ops and **denied to the finance and auditor roles**, while `org.unit.read` and `org.team.read` are **allow for all six**; all three are `scope: () => ALLOW_ALL`. **So an ordinary employee can read the whole directory and the whole org chart, by design** — `policies.ts` says it in terms ("the org chart is PUBLIC_INTERNAL … the ROW scope is open and the field mask is what withholds anything sensitive"), and the matrix draws the contrast with `config.*`, where ADR-0005(a) makes reading a policy row a privilege. The line these tools sit on is therefore *public structure* vs *the reporting graph* vs *configuration*, not "company data vs personal data". **Three things the checking changed.** (a) `org_team_members` was in the backlog as buildable and **is not**: its rows would be employees under resource `team`, where `employee_number` and `full_name` are unregistered, so every row would mask to `{}` — it needs a registry decision first, not a SELECT list (DEC-138 again). (b) `org_teams` returns `department_code`, not `department_name`, because only the code is registered on `team` and an unregistered column is dropped silently. (c) The backlog's claim that *"two of six roles get an empty catalogue … finance and auditor are currently offered nothing at all"* is **wrong for the seeded users and was corrected in the document**: roles are additive and both hold `employee` as well (`employee + finance`, `auditor + employee`), so they were already offered 21 and 23 tools. It would hold only for a hypothetical pure-role account, which is what the matrix cells describe. **Test obligation met per tool**: `familyOf` in the red team now maps `department` and `team` to the *directory* reach rather than falling through to *reporting* — without that, section 1 passed vacuously for them, because a department row names its head by name and `peopleIn` looks for employee numbers. New section 6e asserts, for every role, that a directory answer carries **no** `neverInList` or narrower field (phone, personal email, date of birth, address, gender, blood group, emergency contact, exit reason), plus a positive control that an employee CAN read a colleague's work email — and, in the same block, that the same employee **cannot** read that colleague's leave balance. Suite 956 → **1089 passed, 0 failed** | Human (product owner) + Claude | — |
| DEC-146 | 2026-09-10 | **Three questions that returned nothing had three different causes — two were missing team tools, one is a seed gap, and one whole domain is unbuilt** | "attendance details of all employees", "who took leave?" and "team effort by person" all answered "nothing matched", which reads as one fault and is not. **(a) Missing team tools, and DEC-144 is why they were missing.** `attendance_summary` and `leave_ledger` already query the whole organisation — `fn_attendance_summary` returns one row per employee and the scope predicate filters it — but DEC-144 made them default to the ASKER when no person is named. That is right for "how many days was I late" and useless for "attendance for all employees", and `attendance_team_today` was the only multi-person attendance tool and covers a SINGLE DAY, which had no rows because nobody has punched in yet today. So a wide-scope role had nothing to land on. Two tools added, `attendance_team_summary` and `leave_taken_by_person`, both `subjectDefault: 'scope'` and both reusing the action, resource and `scope()` of the tool they mirror — no new permission, and an ordinary employee gets exactly one row because that is what the predicate returns for them. **Worth stating plainly: DEC-144 did not break these questions, it exposed them.** Before it, the same questions returned everyone's rows and the model narrated the first as though it were the asker's, which was a wrong answer rather than an empty one. **(b) "Who took leave" had to read the LEDGER, not `leave_request`.** The two are different questions: a request records an application and its approval, the ledger records what was consumed, and CLAUDE.md rule 6 is why they can diverge — leave granted by HR, adjusted or migrated in has a `take` entry and no request behind it. In the seeded database the difference is total: **`leave_request` has ZERO rows** while the ledger holds 4 days for EMP001 and 2 for EMP003, so the question returned nothing from a system that plainly had the data. `leave_taken_by_person` reads the ledger and its note says so. **The empty `leave_request` table is a SEED gap, not a code fault** — `infrastructure/db/seeds/demo.sql` creates ledger entries for taken leave without the requests that would normally produce them, so every request-shaped question ("who applied", "what is pending", "who is off next week") is unanswerable in the demo regardless of tooling. Recorded here rather than fixed, because changing seed data changes every demo and every screenshot. **(c) "Team effort by person" cannot be answered at all**: the `work` domain has zero tools although 38 work-log entries across 3 people exist, and all 18 backlogged work tools are blocked on field-registry entries for `work_log`, `timesheet`, `task`, `project` and `project_effort` — the backlog's own prerequisite #1. **Also found**: attendance data stops at 2026-09-08 while the business date is 2026-09-10, so any "today" question is empty on the demo data — correct behaviour, but it makes a today-scoped tool a poor default for a demo. Catalogue 28 → 30; red team 1089 → **1159 passed, 0 failed** | Human (product owner) + Claude | Seed gap: before the next demo |
| DEC-147 | 2026-09-10 | **The field-registry pass for work, and the five tools it unblocked — the assistant now answers a manager's team questions without anybody writing a role check** | The `work` domain had **zero tools** while 38 log entries across 3 people sat in the database, so "team effort by person" was unanswerable. The blocker was the backlog's own prerequisite #1: `fieldMask` returns an **empty set** for an unregistered resource type and `applyMask` drops what it does not know, so a tool over `work_log`, `project_effort` or `timesheet` would have returned rows of `{}` with no error anywhere. Three types registered, and the columns were taken from the existing screens (`/work-log`, `/team/effort`, `/timesheet`) rather than invented. **`work_log.description` is registered `SELF_ONLY`, which is the point of the whole pass**: ADR-0020 §6 carries ADR-0014's rule verbatim and unamendably — *"no narrative work-log content reaches anyone but its author"* — and until now that was a promise kept by remembering to leave a column out of a SELECT list. `SELF_ONLY` also sets `neverInList`, so prose survives only in a `selfOnly` tool masked with `inList: false` (DEC-143): **two independent controls, and the red team asserts the outcome rather than either mechanism.** Five tools: `work_my_log` (selfOnly, the author's own notes), `work_effort_summary` (minutes by project, self-default per DEC-144), `work_team_effort`, `timesheet_status`, `timesheet_team_status`. **The role differentiation is entirely in the actions and needed no code**: `work.team_effort.read` is **deny for `employee`** and allow for manager/hr_admin/hr_ops, so an employee is never offered that tool and is refused it if named directly, while a manager gets their subtree and HR the organisation — verified per role, not assumed. `work.project.read` and `work.project_effort.read` resolve through the **project graph** (membership, not a reporting line) and `project`/`task` remain unregistered, so no tool over them was built by analogy; both are named in the backlog instead. **`timesheet_team_status` deliberately reuses `work.timesheet.read` rather than `work.timesheet.approve`**, because that action carries `isSelf` as a deny-override and the `permittedTools` probe would be guaranteed to fail — the silent-empty-catalogue trap DEC-136 found the hard way. **No ordering by effort**: `work_team_effort` sorts by NAME and project code, never by minutes, because effort per person is exactly the shape that becomes a league table if sorted by magnitude, and §6 forbids a tool that ranks or orders people; the `/team/effort` screen sorts by minutes within one person, which is a different thing, and the assistant does not copy it. New red-team section 6f asserts, per role, that no tool but `work_my_log` returns a `description`, that the author DOES get their own notes back (or the registration is merely hiding the column from everyone and proves nothing), that an employee is refused `work_team_effort` outright, and that a manager's result is alphabetical. Catalogue 30 → 35; authz matrix 435 → **453**; red team 1159 → **1489 passed, 0 failed** | Human (product owner) + Claude | — |
| DEC-148 | 2026-09-10 | **A scope-narrowed answer now says so, and a streamed answer call falls back to non-streaming** | An employee asked *"can you tell me September attendance details of all employees?"* and got **"Your records hold 1 record for that, but the answer could not be written just now."** Two independent faults in one sentence. **(a) The silence.** The tool returned exactly one row — the asker's own — because `scope()` did its job, and **nothing said the question had been narrowed**. That is the same family as DEC-142 (a person out of reach reported as "nothing matched") and DEC-144 (the wrong subject entirely): an authorization outcome presented as a fact about the data. The fix is a **note, not a refusal**: refusing would be wrong, since a manager asking the identical question has a legitimate partial answer, and the instruction was to make the assistant work per role rather than block. Every `subjectDefault: 'scope'` answer now carries its own coverage — *"This covers your own records only. Your account does not have access to records for other people…"* for one, *"This covers the N people your account has access to, which may be fewer than the whole organisation"* for more — and it reaches the model as "Note that must be conveyed", so it lands in the prose the user actually reads. **Derived from the RESULT, not from the policy**: counting the distinct people returned is accurate by construction, needs no second query and no reading of scope internals, and stays right if a policy changes underneath it. Self-defaulting and person-less tools get no note, because they were never about anybody else. **(b) The failed answer call was mine.** DEC-141 made only the ANSWER call stream (`stream: true` + `stream_options`); route and select do not. The symptom matched that exactly — a tool was selected and run, one row came back, and only the sentence failed. `stream_options` is a **capability, not an argument we chose**: an Azure deployment, a gateway or an older model can reject it while accepting the identical non-streamed request, and a 4xx is normally not worth retrying. So the single retry now **drops streaming** when the streamed attempt failed with nothing yet emitted, converting a dead answer into one that arrives all at once; it warns once naming `HRM_LLM_STREAM=false`, which skips the wasted first attempt for a deployment that can never stream. Red team 1489 → **1492 passed, 0 failed**, with three assertions that the note appears when narrowed, states the count when wider, and is absent on self-defaulting tools | Human (product owner) + Claude | — |
| DEC-149 | 2026-09-10 | **A default-deny mask guards against returning too much and says nothing about returning too little — two tools were silently returning less than they knew** | An hr_admin asked *"joining date of Priya Menon?"* and was told *"the records do not show Priya Menon's joining date"* by a directory that knows it perfectly well: `joined_on` is **PUBLIC in the registry** and was simply absent from `people_directory_lookup`'s SELECT list. The model behaved correctly — it reported what it was given rather than inventing a date — and that is precisely what makes the failure mode dangerous: an omitted column is **indistinguishable from absent data** to every layer above the SQL. `joined_on`, `confirmed_on` and `employment_type` added there, `joined_on` to `people_department_roster`; no mask, policy or scope change, because none was wrong. **The generic check written to catch that class then found a second, pre-existing instance immediately**: `leave_holidays` returned five rows that the mask emptied to `{}`, so "what are the holidays" came back as five blank records with no error anywhere — the exact silent failure DEC-138 predicted. It declares `leave.balance.read`, whose resource IS `leave_balance` (ADR-0020 §1 forbids inventing an action for the calendar), and `holiday_on` / `name` / `is_optional` were never registered on that type. Registered now, with `name` aliased to `holiday_name` so a bare `name` on a type shared with leave balances cannot confuse the next reader — the same precedent as the `fn_attendance_summary` aggregates sharing `attendance_day`. **The red team gained the assertion that would have caught both**: any tool that answers with ROWS must answer with COLUMNS, per role, across the whole catalogue — an unregistered resource type now fails the gate at once instead of shipping as blank records. Plus a named expected-column set for the directory, and a check that a joining date comes back as a date-only string (rule 5). Red team 1492 → **1597 passed, 0 failed**; authz matrix 453/453 unchanged | Human (product owner) + Claude | — |
| DEC-150 | 2026-09-10 | **Adding a routing domain hid the tools of another one; and `present_days` silently includes late days while `wfh_days` does not** | Two employee-facing wrong answers, both from the same root cause class: a fact the system holds, presented as a fact it does not. **(a) "Who is my manager?" → "Nothing matched."** `me_profile` has carried a `manager` column all along and lists that exact question as an example — but **the router picks ONE domain** and the selection step sees only that domain plus `cross` (DEC-127). A reporting-line question reads as a `people` question, and until DEC-145 the `people` domain had **no tools at all**, so nothing could route there. Adding four tools made it routable and, in the same stroke, made every `me`-domain tool invisible to anything that landed on it — so the question had to be answered by a directory LOOKUP that needs a name it was never given. **The general lesson is worth more than the fix: adding a domain silently changes what the router can choose, so a question answerable in `me` must also be answerable in any domain it plausibly routes to.** Two backlog tools built to close it, `people_manager_of` (defaults to the asker) and `people_who_reports_to` (the manager's most basic question, which the catalogue could not answer at all). **(b) "How many days did I work this month?" → 5, when the screen showed 6.** `fn_attendance_summary` counts `present_days` as `status IN ('present','late')`, so **late days are already inside it and `late_days` is a subset, not an addition**, while `wfh` is its own status and is **outside** it. Three columns whose overlap is nowhere stated is a puzzle, and a model solving a puzzle silently gets it wrong silently. Both summary tools now compute `days_worked = present_days + wfh_days` and carry a note saying that present and late must never be added. The arithmetic belongs in SQL, not in the model. **Two implementation faults found by the gates rather than by reading**: `people_who_reports_to` built its WHERE clause conditionally while passing a fixed parameter list, and Postgres answered `could not determine data type of parameter $2` — a 500, so the tool was dead for everybody; every placeholder is now referenced on every path. And `people_manager_of` failed section 6d until marked `relatedPeople`, because its row is about the asker and names their manager — the same shape as `me_reporting_chain`. Catalogue 35 → 37; authz matrix 453/453; red team 1597 → **1684 passed, 0 failed** | Human (product owner) + Claude | — |
| DEC-151 | 2026-09-10 | **A multi-row answer is rendered as a bullet list, from a two-construct markdown subset the panel parses itself** | Four people of attendance arrived as one paragraph — *"Vishnu Ravi was present for 5 days, late 1 day, worked from home 1 day, and absent 0 days. Priya Menon was present for 6 days…"* — a wall of numbers nobody reads. This is a **presentation** change and nothing else: every figure still comes from the masked rows, and the rules about never estimating and never ranking are untouched. The answer prompt now asks for **`**bold**` and `"- "` bullets and nothing else** — one bullet per record, a lead line above them, prose for a single fact — and says outright that anything else is shown to the user as raw characters, so what the model emits and what the panel renders cannot drift apart. **The parser is a trust boundary, and that decided its size.** It renders MODEL OUTPUT, so it builds React elements from strings and never touches `dangerouslySetInnerHTML` — there is no HTML injection path at all, and the worst a hostile value can do is look odd (`<script>` renders as visible text). A markdown library would be a dependency to justify (CLAUDE.md) and a far larger surface for one panel; headings, tables, links and images are deliberately absent, and an unrecognised construct falls through as literal text rather than vanishing. **Streaming needed one non-obvious thing.** Fragments arrive mid-token, so an opening `**` lands before its partner and would flash as asterisks on every bolded name. The obvious fix — strip a trailing ``**…`` with a regex — is WRONG and was caught by a check rather than by review: it removes the CLOSING pair of a completed bold, so `- **Vishnu Ravi** - 6 days` rendered as `- **Vishnu Ravi - 6 days`. Only an ODD number of pairs means one is open, and then it is the last that has no partner; `hideOpenBold` counts. The caret rides the last block so it trails the text being written instead of sitting alone under a list. Web build, `tsc`, i18n 14/14 and red team **1684 passed, 0 failed**, all unchanged — no API, tool, mask or scope was touched | Human (product owner) + Claude | — |
| DEC-152 | 2026-09-10 | **Task tools, and the status value a model invented because nothing told it the real ones** | An hr_admin asked *"is anybody have any pending tasks?"* and got "nothing matched" from a database holding **eight outstanding tasks, six of them assigned**. Two causes. **(a) No task tool existed.** DEC-147's registry pass covered `work_log`, `project_effort` and `timesheet` and deliberately stopped short of `task`, so any tool over it would have masked to `{}`. `task` is registered now and two tools follow the shape the action already dictates: **`work.task.read` takes the REPORTING graph and scopes on `assignee_employee_id`**, which `graphs.ts` calls load-bearing rather than inconvenient — an unassigned task has no subject, so the predicate excludes it **by construction** rather than by a condition somebody must remember. Two of the nine seeded tasks are unassigned and are therefore invisible here; both tools **say so in a note**, because "nobody has pending tasks" and "nobody I can see has been ASSIGNED one" are different answers and the difference must not be silent. The project-shaped cut — "what is open on ATLAS", including unassigned work — is `work.project.read` and its MEMBERSHIP graph; not built by analogy, and `project` remains unregistered. `work_open_tasks` counts per person and is ordered by employee number, never by count: "who has the most open tasks" is a league table however phrased (§6). **(b) The model invented a timesheet status.** Reaching for the nearest tool it had, it passed `status: 'pending'` to `timesheet_team_status` — and the real values are `draft`, `submitted`, `under_review`, `approved`, `returned`. A free-text filter that matches nothing returns an empty result that reads as a fact about the data. The parameter description now names every valid value and states outright that there is no "pending" status. **A note on the matrix count**: it reads 452 rather than 453 because the "unregistered resource yields no fields" check DISCOVERS its subject rather than hardcoding it, so registering `task` removed one iteration — the suite's own guard that the list must not empty still passes, with `project`, `designation`, `audit_event` and `identity` left. Catalogue 37 → 39; red team 1684 → **1777 passed, 0 failed** | Human (product owner) + Claude | — |
| DEC-153 | 2026-09-10 | **No asterisk ever reaches the reader, and a duration is formatted in SQL rather than converted by the model** | Two presentation faults in one answer. **(a) `**` on screen.** The renderer matches `**bold**` and left anything it could not parse as literal text — defensible, and wrong for a reader, who sees `**Total records found: 5**` and calls it a bug. Bold is a nicety; leaking punctuation is not, so an unparsed pair now **degrades to plain text** rather than to noise: `stripStars` removes every surviving `**` from the segments between matches. A SINGLE asterisk is deliberately untouched — it is a footnote mark, a multiplication sign or part of a value, and removing it would corrupt content rather than tidy it. Verified against the exact strings from the screenshot plus `***emphasis***` and a pair the regex cannot span; none leaks. The prompt also now forbids bolding a whole line, because a bolded lead reads as a heading and headings are not available. **Note the panel in that screenshot was still serving the pre-DEC-151 build** — the bullets were not rendering as a list either — so part of what was reported is a stale asset rather than a defect; the strip is worth having regardless, since it is the only thing that holds when the model formats in a way the parser does not expect. **(b) Minutes where people think in hours.** `2940 minutes` is how the column is stored and not how anybody asks. The conversion is arithmetic, and DEC-150 already established that arithmetic belongs in SQL rather than in a model that gets it wrong silently — so `time_spent` (work) and `worked_time` (attendance) are computed as `49h 00m` in the query, alongside the raw minutes, which stay for a question that actually asks for them. The format is **the one the attendance screen already uses**, so an answer and the screen beside it agree. The prompt says to quote the formatted value as given and never to convert between the two. Registry entries added for both, per DEC-138 — a derived column the registry does not know is dropped silently. Gates unchanged: authz 452/452, i18n 14/14, red team **1777 passed, 0 failed**, web build clean | Human (product owner) + Claude | — |
| DEC-154 | 2026-09-10 | **Every timestamp the assistant returns is rendered in the CONFIGURED company timezone, in SQL** | An employee asked about late days and was told they arrived at **04:17** on a day the attendance screen shows as **09:47**. The gap is exactly 5h30m: `first_in_at` is a `timestamptz`, it reaches JavaScript as a `Date`, and it leaves as a UTC ISO string — so the model was handed 04:17 and reported it faithfully. The worked minutes were right the whole time, which is what made it look like a partial fault rather than a units one. **The screens were never wrong**: `attendance/page.tsx` and `punch-card.tsx` format with `timeZone: 'Asia/Kolkata'`. The assistant renders no times of its own, so the conversion has to happen in SQL while the value is still a timestamp — `localTime` and `localDateTime` in `catalog.ts`, applied to `first_in_at`, `last_out_at`, `punched_at`, `submitted_at`, `decided_at` and the ledger's `created_at`. **The zone is read from `org_setting company.timezone`** with the same COALESCE fallback `fn_business_date()` and migration 0012 use, because CLAUDE.md rule 11 makes the timezone configuration and a literal would have hardcoded a policy into twelve SELECT lists. This is the same family as `db.ts`'s DATE parser, whose comment already says the bug is "entirely in the driver's helpfulness" — DATE was fixed globally in 0.x, `timestamptz` never was, and nothing had needed it until a tool started handing raw values to a model. **Two faults of my own, both caught by gates rather than by reading.** The blanket string replace that applied the formatter also rewrote the `ORDER BY` clauses into `ORDER BY to_char(...) AS punched_at DESC` — invalid SQL, a 500, and `attendance_punches` and `leave_ledger` dead for every role until the red team said so; ordering now uses the raw timestamp, which sorts by instant rather than by rendered string and is more correct besides. And the answer prompt's bullet template read `- **Who or what** - the figures…`, which the model copied **literally** as a label; a placeholder that can be mistaken for content is a bad placeholder, so it is now a worked example with real names. Gates: authz 452/452, red team **1777 passed, 0 failed** | Human (product owner) + Claude | — |
| DEC-155 | 2026-09-10 | **The model does no arithmetic at all; totals are computed in SQL — and the first attempt at that rounded** | "Who worked more?" reported **106h 00m** as the total of 49h, 45h30 and 36h30, which is 131h. Nothing failed and nothing looked wrong: the model added three numbers and got one of them wrong, silently. The per-person figures were themselves sums the model had done across project rows — it happened to get those right, which is worse, because it makes the method look sound. **The rule is now absolute in the prompt — DO NO ARITHMETIC: never add, subtract, total, average or convert; every number written must appear verbatim in a record or in the note; if a total is not given, do not state one.** That is only safe if the figures a question needs are actually provided, so `work_team_effort` gained `person_time` / `person_minutes` — a window rollup of that person across every project, on each of their rows — and its note now states the overall total, computed in integer minutes in our own code from the rows it just produced. This is DEC-150's lesson applied a second time and generalised: arithmetic belongs in SQL, and a model asked to add is a model that will eventually add wrong. **The first implementation was itself wrong, in a way worth recording.** `sum(sum(minutes)) OVER (PARTITION BY ...)` returns **NUMERIC**, not bigint, so `/ 60` divided as a decimal and `::int` **rounded** — 2730 minutes rendered as `46h 30m`, an hour more than the project rows beneath it added to. `time_spent` is safe only by accident, because plain `sum(int)` is bigint and divides as integers. Casting to `int` before dividing fixes it. The red team now asserts the identity rather than fixture numbers — person totals counted once each must equal the sum of every row, and each formatted `person_time` must match its own `person_minutes` — because the bug lived entirely in the formatting and an integers-only check would have missed it. **Also confirmed, with evidence, that the reported `**` is a stale asset**: the renderer is present in `.next/static/chunks` from the 01:46 build, and a web server started earlier is still listening on :3100 serving the previous chunks. Gates: authz 452/452, red team **1783 passed, 0 failed** | Human (product owner) + Claude | — |
| DEC-156 | 2026-09-10 | **One tool escaped the timezone fix, "login time" pointed at an empty table, and the catalogue now proves the property instead of relying on a replace** | Three questions, three different states. **(a) A REAL miss.** DEC-154 wrapped every `timestamptz` in `localTime`, applied by a string replace over the paired literal `ad.first_in_at, ad.last_out_at` — and `attendance_late_days` selects `first_in_at` **on its own**, so it was skipped and kept answering in UTC. "Do I have any late in?" still reported 04:17 for a 09:47 arrival. Nothing broke: the column was present, the value was a real time, only the timezone was wrong, and catching it by review would have meant noticing one line across six files. **The property is now asserted over the whole catalogue** — section 6g checks that no value any tool returns to any role matches an ISO instant, so a raw `timestamptz` fails the gate wherever it appears rather than wherever somebody remembered to look. **(b) A SELECTION failure over an empty table.** "What is my login time on September 1?" reads as a punch question, and `attendance_punch` holds **zero rows** — the seed writes `attendance_day` directly — so `attendance_punches` can only ever answer "nothing matched". The answer lives in `attendance_days.first_in_at`, which had it all along (09:12). Descriptions now settle it: `attendance_days` explicitly claims "login time", "sign-in time", "punch in time" and "what time did I start", and `attendance_punches` is narrowed to the raw events and points at `attendance_days` for anything about when a day started or finished. **The empty punch table joins the DEC-146 seed gaps** — recorded, not fixed, because seed data changes every demo. **(c) NOT A DEFECT.** "List of employees?" works and returns all five: `people_directory_lookup` has answered it since DEC-145. That question, and the `**` from the previous report, were both the deployed build — the API on :4000 and the web server on :3100 are running code from before several of these changes. Red team 1783 → **1960 passed, 0 failed** | Human (product owner) + Claude | Seed gap: with the DEC-146 items |
| DEC-157 | 2026-09-10 | **The assistant's standing caveat is removed, and clicking outside the panel closes it** | Two UI changes, both asked for, and both worth a line because they trade against something recorded earlier. **(a) The footnote is gone.** *"Answers are written from your own HR records by an AI assistant. It can be wrong, and it cannot change anything"* was written for DEC-141, when removing the result table left the prose as its own only evidence — the caveat was what replaced the table as the reader's cue to check. Nothing on screen now marks an answer as machine-written: the panel title, "HR assistant", and its subtitle are the only remaining context, and they are not disclaimers. The key is deleted from both locales rather than left unused, so a translator does not maintain copy that renders nowhere. **This is a product call, not an engineering one, and it is recorded here so it is a decision rather than a deletion.** **(b) Click outside dismisses.** It sits against the panel being deliberately NON-MODAL, which the component header explains at length: an assistant is most often read ALONGSIDE the page, checking an answer against the screen behind it, which is why focus is not trapped. Dismiss-on-outside-click means any interaction with that page — a scrollbar drag, selecting a figure to compare — now closes the panel. Nothing is lost when it happens: the conversation lives in component state and reopening shows every previous turn. `pointerdown` rather than `click`, because a click fires after the button up, by which time a control inside the panel may have unmounted and its target would test as outside; and the launcher is excluded, or clicking it while open would close the panel here and the button's own handler would toggle it straight back. Escape still closes and still returns focus to the launcher. Gates: web build clean, `tsc` clean, i18n 14/14 with both locales still at parity | Human (product owner) + Claude | — |
| DEC-158 | 2026-09-10 | **The router's "do not narrow" fallback narrowed to `me`, and a filter that matched nothing reported an empty organisation** | "List the employees?" answered *"there are no records of employees"* from an instance that has `people_directory_lookup` and returns all five. **The deployed build was current — measured, not assumed**: `/assistant/capabilities` on the running API offered 36 tools including every one added since DEC-143, and `/assistant/run` returned 5 rows. So the fault was selection, and there were two. **(a) `route()` contradicted its own comment.** The comment reads *"An unrecognised answer must not narrow anything - fall back to the widest pool rather than silently dropping every tool outside a domain the model invented"* and the code returned `present[0]` — whichever domain the FIRST registered tool belongs to, which is `me`, because `tools-me` is imported first. Any router reply the parser could not match exactly ("the people area", a trailing sentence, a translated word) therefore restricted selection to the asker's own record, where nothing can list employees. The `permitted.length <= 12` line had the same defect for the opposite reason: it meant "small enough to select over directly" and narrowed to one domain instead. `route()` now returns `Domain | null`, null meaning do not narrow, and the caller uses every permitted tool. **The lesson generalises past this bug**: a comment describing behaviour the code does not have is worse than no comment, because it stops the next reader looking. **(b) An unmatched filter read as a fact about the data.** The model reached `people_department_roster` with `department: "employees"`, matched no department, and returned zero rows — indistinguishable from an empty organisation, and the same shape as the invented timesheet status in DEC-152. The tool now names what it could not match and lists the departments the caller can actually see, from the SAME scoped query, so it discloses nothing a directory read would not: *"No department matched \\"employees\\". The departments you can see are: Engineering, Human Resources."* `people_directory_lookup` also now claims the plain question outright — "list the employees", "who works here" — rather than describing itself only as a lookup. Red team **1960 passed, 0 failed** | Human (product owner) + Claude | — |
| DEC-159 | 2026-09-10 | **`meta_capabilities` and `people_headcount`; and text-to-SQL is refused, with the reason stated** | A sweep of the 89-candidate backlog against the 39 built tools leaves **44 A-marked candidates unbuilt**. Two were picked first because they answer questions nothing else could. **(a) `meta_capabilities`** — "what can I ask you?" had no tool at all, which is the worst gap a tool-calling assistant can have: a question outside the catalogue is refused with what is NOT there and never with what IS. It reads the asker's OWN catalogue through `permittedTools`, so it cannot advertise a tool they would then be refused. **The action is a reuse and the reasoning is recorded**: its equivalent screen, `GET /assistant/capabilities`, is gated by `@Authenticated()` and nothing more, so there is no action — and §1 forbids inventing one. `org.unit.read` is the narrowest EXISTING action all six roles hold, and the closest in kind; `leave_holidays` reusing `leave.balance.read` is the precedent. **(b) `people_headcount`** — "how many people work here" had no answer **because of DEC-155**, which forbade the model doing arithmetic after it totalled 49h + 45h30 + 36h30 as 106h. That rule also removed its ability to count rows, so a directory listing five people could not say "five". A count question needs a tool that counts. No k-suppression, on `org_department_tree`'s reasoning: ADR-0017(d) governs aggregates crossing an individual boundary, and a headcount says how many, never which. Both tools shipped with their derived columns **unregistered**, so every count came back `undefined` and every capability row empty — DEC-138's trap caught for the third time, and the argument for a lint over a convention. **TEXT-TO-SQL IS REFUSED, and not on preference.** ADR-0020 evaluated and rejected it, and its *Reconsider when* requires a **superseding ADR**, an amendment to `security-guidelines.md`'s ban on string-concatenated SQL, a database grant change and a Postgres behaviour spike. The decisive fact is **OR-29**: the API connects as the database OWNER, so the least-privilege role the whole sandbox design rests on is not in force. Generated SQL would run as owner — past `scope()`, past the field mask, past the payslip revoke, past the 2060-assertion gate, all of which exist to make exactly that impossible. Twenty minutes buys a demo that bypasses every authorization property this catalogue is built on. The ADR's own instrument is the `no_tool` log, which is evidence rather than anticipation; surfacing it is the cheap next step. Red team 1960 → **2060 passed, 0 failed**; catalogue 39 → 41 | Human (product owner) + Claude | Text-to-SQL: only via a superseding ADR, and not before OR-29 |
| DEC-160 | 2026-09-10 | **A bubble on both sides of the conversation, and the visual change was LOOKED AT rather than asserted** | The panel gave the question a bubble and the answer none, so a reply read as loose text under a message rather than as a turn in a conversation. Both sides now share one shape: dark on the asker's side, a light `ink-50` surface with an inset ring on the assistant's, `rounded-2xl` with the corner nearest the speaker clipped, and a `max-w` under full width - the ragged edge is what makes it read as something somebody said. **Direction-awareness was a constraint, not a detail.** `justify-end` and the LOGICAL radii `rounded-ee-sm` / `rounded-es-sm` flip with `dir`, so in Arabic the bubble and its clipped corner move to the other edge with no second rule; a physical `mr-`/`text-right` would not, which is exactly what `i18n:test` R1 exists to stop, and it passes. The assistant surface is defined **once** as `ASSISTANT_BUBBLE` and shared by all four states - thinking, answer, refusal, nothing-matched - because four copies of a class list drift, and a reply that changes shape as it resolves reads as a glitch. The waiting state now occupies the same bubble the answer will fill, so the reply grows in place instead of the layout jumping on the first token. The note stays OUTSIDE any bubble: it is the system talking about the answer - period covered, whose records, what was suppressed - and giving it the answer's surface would make it read as more of the reply. **Verified by screenshot, over CDP against the real stack** (the DEC-125 approach, no Playwright): a spare web server on :3101 from the fresh build, driving the operator's own API, produced a real turn - dark question bubble, light answer bubble, `**Vishnu Ravi**` rendered as bold, the bullet rendered as a list, no stray asterisks, and the right figure. That single screenshot independently confirms DEC-151's renderer, DEC-153's asterisk strip and DEC-140's answer-from-rows all working together, which no amount of `tsc` output would have shown. Gates: web build clean, `tsc` clean, i18n 14/14 including R1 | Human (product owner) + Claude | — |
| DEC-161 | 2026-09-10 | **`meta_capabilities` drops its trailing note** | *"41 lookups are available to you, across 5 subjects. These are examples, not the only wordings that work"* was chrome about the answer rather than the answer. The subjects and their examples already say everything a reader needs, and the caveat hedged a list that is not a promise in the first place. Removed for the populated case; the EMPTY case keeps its note (*"Your account holds no permissions the assistant can act on"*) because without it the reply would be nothing at all. Same class as DEC-157's footnote removal: assistant chrome the product owner does not want, taken out rather than reworded. A tool's `note` reaches the model as "a note that must be conveyed", so anything left in one is text the reader WILL see - which makes a note a deliberate choice each time, not a place to put remarks. Red team **2060 passed, 0 failed**, unchanged | Human (product owner) + Claude | — |
| DEC-162 | 2026-09-10 | **Seven demo questions per role, each MEASURED through the real endpoint - and `meta_capabilities` narrowed after it stole a question from another tool** | Twenty-one candidate questions were run through `POST /assistant/ask` against the seeded database, per role, recording the tool each one actually reached. **19 worked; the two that did not are the interesting part.** `docs/demo/assistant-questions.md` holds the result, and it is a record of what happened rather than a list of what ought to work - which matters because **selection is the one property no offline check can prove**: `assistant:redteam` deliberately drives no model, so it proves authorization and says nothing about whether a question finds its tool. **(a) A regression I introduced one decision earlier.** `meta_capabilities` (DEC-159) described itself as answering *"what do you know about"* and *"can you tell me about my leave"*, and the model then chose it for **"what is my phone number?"** - a question `me_contact_details` had been answering correctly. A tool that advertises itself as the answer to open-ended questions competes with every tool that holds an actual answer, and it wins whenever the router declines to narrow (DEC-158's fallback). The description now says what the tool is NOT for, in as many words: a question naming any actual subject belongs to that subject's tool, even when phrased as "what do you know about X". **The build is ready and this specific question is NOT re-verified**, because confirming it needs the running API restarted and its key exists only in that process's environment. **(b) "Who is off next week?" cannot work on this seed** - `leave_request` has zero rows (DEC-146), so every request-shaped question is unanswerable regardless of tooling. Replaced in the demo list with *"What is Vishnu Ravi's leave balance?"*, which is worth demonstrating deliberately: it shows a manager's REACH, and the same question asked by an employee about their manager is refused. **(c) Two HR questions sit against §6 and are flagged rather than quietly kept or quietly dropped.** "Who has taken the most leave?" and "who has logged the most working hours?" both work, and no tool ranks anybody - `work_team_effort` orders by name - but the ANSWER is a ranking of people by output, which is the shape §6 exists to prevent. Recorded as asked, with a non-ranking alternative named, because which questions belong in a demo is a product decision | Human (product owner) + Claude | The §6 tension in the two HR questions |
| DEC-163 | 2026-09-10 | **`meta_capabilities` answers with SUBJECTS only, and an unanswerable question keeps the plain refusal** | Two complaints about the same tool. **(a) It listed twenty example questions** - four per subject - which reads as a manual rather than an answer, and goes stale the moment a tool is reworded. It now returns the subject names alone, computed per role from `permittedTools`: *Attendance · Leave · Your own record · People and the org chart · Work and timesheets*. The subject is what a reader actually needs - it says where to point a question, and the assistant takes any wording once it is pointed the right way. `you_can_ask` is removed from the registry too rather than left as a dead entry. **(b) An unanswerable question was getting a capabilities LIST instead of a refusal.** The wanted text - *"That is outside what this assistant can answer. It covers leave, attendance and your own employment record. Ask \\"what can you help me with?\\" for the list."* - is already the `no_tool` refusal, verbatim. Nothing to change there: the fault was that `meta_capabilities` was being SELECTED for questions it should never have taken, which DEC-162 addressed by narrowing its description to say what it is not for. The two fixes compound - a tool that both stops volunteering for data questions and, when it does answer, returns five words rather than twenty questions, is much harder to mistake for an answer. **Worth stating because it will come up again**: the `no_tool` message names "leave, attendance and your own employment record", which is now incomplete - the catalogue also covers people, work, tasks and timesheets. It is left verbatim because it is the wording that was asked for; a later pass could compute the subject list from the actor's own catalogue and have one source of truth instead of two. Gates: authz 452/452, red team **2060 passed, 0 failed** | Human (product owner) + Claude | The stale subject list in the `no_tool` message |
| DEC-164 | 2026-09-10 | **The assistant's production key is a bind-mounted FILE named by one variable, and the plain `HRM_LLM_API_KEY` is not declared on the container at all** | ADR-0020 deferred exactly one thing to the deployment - how the provider key reaches a production container - and `prod.env.template` said in terms that the assistant "CANNOT BE TURNED ON FROM THIS FILE", because the `api` service declared none of its variables. That is a safe state but not a finished one: the next operator to need the feature would have improvised, and the improvisation most likely to be reached for is the one `security-guidelines.md` bars - a plain environment variable, readable in `docker inspect`, `/proc/<pid>/environ`, child processes and one careless `console.log(process.env)`. So: `HRM_LLM_KEY_HOST_PATH` names a file **on the VM**, compose mounts it read-only at a **fixed** `/run/secrets/hrm_llm_api_key`, and the container-side path is deliberately NOT a second variable - it is the far half of one mount, and a second knob could only ever disagree with the first. **`HRM_LLM_API_KEY` is undeclared on the service**, so the plain-variable path does not exist in a deployment and cannot be taken by accident; it remains the local-development shortcut (DEC-129) and nothing else. The mount defaults to **`/dev/null`**, which reads as an empty file: a bind mount cannot be conditional in compose and an empty source is a syntax error, so an always-present harmless source is what keeps this to one variable. Three failure modes were designed out rather than documented: Docker creates a **directory** at a missing bind source, a root-owned `0400` file is unreadable by the container's `node` user (**uid 1000**), and an empty file is indistinguishable from no key - each produces a healthy-looking API that refuses every question, so `deploy.sh` now stops the deploy on all three instead. **Nothing is switched on**: `HRM_ASSISTANT_ENABLED` still defaults to `false` and ADR-0020's release gate - a 100% `assistant:redteam` score, no waiver - is untouched. Having somewhere to put the key is not clearance to use it | Claude | Before the assistant is first enabled in production |
